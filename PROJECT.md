# PROJECT.md

Reference doc for how this repository is organized and how the app works under the hood. For "how do I run this," see [README.md](README.md). For "how do I contribute a change," see [CONTRIBUTING.md](CONTRIBUTING.md).

## What this is

Open Play Manager is a web app for running pickleball open-play sessions: check-in with player photos, automatic doubles matchmaking, live scoring by court, standings, and a paid access-code gate for creating new sessions. It started as a Claude.ai artifact prototype and now lives here as a standalone Vite + React project backed by Supabase.

## Repository layout

```
openplay-manager/
├── index.html                    Vite entry HTML
├── package.json                  Scripts and dependencies
├── vite.config.js                Vite configuration, incl. the VitePWA plugin (manifest + service worker) — see Architecture below
├── .env.example                  Template for your Supabase URL + anon key
├── public/
│   ├── favicon.svg                CONNECT.PH-inspired mark (navy field, orange "C" ring + connector dot)
│   ├── favicon.ico                16x16 + 32x32, PNG-in-ICO
│   ├── favicon-16x16.png
│   ├── favicon-32x32.png
│   ├── icon-192.png                PWA icon (purpose "any")
│   ├── icon-512.png                PWA icon (purpose "any" + "maskable")
│   └── apple-touch-icon.png        iOS home-screen icon (180x180)
├── scripts/
│   ├── generate-pwa-icons.mjs    Regenerates every icon/favicon above (hand-encoded PNG + ICO, no deps) from the CONNECT.PH palette
│   └── run-simulation.mjs        CLI entry point for RotationSimulationEngine — runs a full simulated session and prints the results (node scripts/run-simulation.mjs); --courts=2,3,4 runs and compares multiple court counts in one go
├── src/
│   ├── main.jsx                  App entry point — loads the storage shim first
│   ├── App.jsx                   Thin wrapper around the main component
│   ├── PickleballOpenPlay.jsx    Top-level app state/logic and screen router
│   ├── storage.js                Supabase-backed stand-in for window.storage
│   ├── styles.js                 Design tokens and the shared style object
│   ├── lib/
│   │   ├── supabaseClient.js     Supabase client setup (reads VITE_ env vars)
│   │   ├── constants.js          Shared constants and default state shapes
│   │   ├── random.js             uid()/shuffle() — split out to avoid a circular import with src/engines/
│   │   ├── utils.js              Pure helpers: codes, formatting, and the matchmaking entry points (refreshNextMatchups, regenerateNextMatchups, recordRotationHistory)
│   │   ├── winnerPoolRound.js    Court-pairing orchestration for Winner Pool Rotation mode (holds a finished court until its pair partner also finishes, then pools/redeploys)
│   │   ├── progressiveSkillPhase.js  Session-progress/phase calc for Progressive Skill Rotation (mentorship/transition/competitive), with organizer-configurable phase boundaries (buildPhases) — feeds ProgressiveSkillRotationStrategy's pairing choice, see below. progressiveSkillPhaseFor() lives here (not in PickleballOpenPlay.jsx) so headless callers can use it too
│   │   ├── performanceRating.js  Session-only Performance Rating (win %, point differential, streak) shown next to players in Standings and Scorer's waiting players list
│   │   └── simulation/
│   │       └── RotationSimulationEngine.js  Headless simulation of a full session under Progressive Skill Rotation — see Architecture below
│   ├── engines/                  Pluggable matchmaking strategies (Strategy pattern) — see Architecture below
│   │   ├── RotationEngine.js                  The interface
│   │   ├── BalancedRotationEngine.js          Continuous-queue mode's team/matchup builder
│   │   ├── WinnerPoolRotationEngine.js        Winner Pool Rotation mode's per-pool team builder (composes BalancedRotationEngine)
│   │   ├── TransitionRotationEngine.js        Progressive Skill Rotation's Transition-phase team/matchup builder — soft beginner+intermediate preference, Performance Rating balances which teams face off
│   │   ├── CompetitiveRotationEngine.js       Progressive Skill Rotation's Competitive-phase team/matchup builder — ignores beginner/intermediate labels, pairs primarily by Performance Rating
│   │   └── ProgressiveSkillRotationStrategy.js  Progressive Skill Rotation mode's team/matchup builder — dispatches by phase: Mentorship composes BalancedRotationEngine, Transition composes TransitionRotationEngine, Competitive composes CompetitiveRotationEngine
│   └── components/               One file per screen/UI component
├── supabase/
│   └── schema.sql                Run once in the Supabase SQL editor to set up the database
├── backend/
│   └── README.md                 Explains the Supabase setup and trust model
├── README.md                     Project overview and quick start
├── CONTRIBUTING.md               Development workflow and conventions
└── PROJECT.md                    This file
```

## Architecture

- **Screens and logic are split apart.** `src/PickleballOpenPlay.jsx` owns the top-level state (current screen, session data, auth) and the functions that mutate it (matchmaking, scoring, standings). Each screen (landing, organizer panel, session setup, live board, check-in, scorer, standings) is its own component under `src/components/`, receiving state and callbacks as props.
- **CONNECT.PH theme, centralized in one `:root` block.** `src/styles.js`'s `fontImport` string (injected once via a `<style>` tag) defines every color the app uses as a CSS custom property, extracted from the CONNECT.PH logo — Navy `--color-primary`, Orange `--color-secondary`, plus `--color-accent`/`-success`/`-warning`/`-error`/`-bg`/`-surface`/`-border`/`-text`/`-text-muted`/`-text-faint`/`-on-primary`. Nothing in the app hardcodes a hex color outside this one block — every style object references a `var(--color-...)`, so retheming means editing these ~15 lines, not hunting through components. The pre-existing `--ink`/`--court`/`--court-dark`/`--chalk`/`--ball`/`--coral`/`--line` names (which the hundreds of style objects throughout this file already reference) are kept as **aliases** onto the new palette (e.g. `--court: var(--color-primary)`) rather than renamed everywhere, so the whole app picked up the rebrand without every style object needing a rewrite. Two things worth knowing: (1) the vibrant brand orange is only ~2.4:1 contrast against a light background — well under WCAG AA's 4.5:1 for text — so `--color-secondary-text` (a darkened ~4.8:1 variant) exists specifically for when orange needs to *be* the text/foreground color on a light surface (see `styles.kicker` vs `styles.kickerOnDark`, used on the light Landing/Access screens vs. the navy session header respectively); (2) Progressive Skill Rotation's phase colors (`--color-phase-mentorship/-transition/-competitive` + `-tint` variants) and the rotating avatar palette (`--color-avatar-1..6`, used by `lib/utils.js`'s `colorForName`) are also defined here rather than as separate hardcoded arrays, for the same "one place to edit" reason.
- **Persistence is abstracted behind `src/storage.js`.** The app calls `window.storage.get/set/delete/list(key, shared)` — a shim with the same signature as the key/value API that Claude.ai artifacts get for free from their host environment. It's backed by a single Postgres table (`opl_kv`) in Supabase; see `supabase/schema.sql` for the schema and `backend/README.md` for setup.
- **Real-time sync via Supabase Realtime.** `storage.js` also exposes `subscribeToKey(key, shared, onChange)`, which opens a Postgres change-stream subscription. `PickleballOpenPlay.jsx` uses this instead of polling — any device's write shows up on every other connected device within about a second.
- **`DEV_ACCESS_CODE` — a permanent, never-expiring access code for the developer's own testing.** `lib/constants.js` hardcodes `"GUILSIGN"`; `submitAccessCode` in `PickleballOpenPlay.jsx` special-cases it before ever touching Supabase — no lookup, no `usedAt` check, always accepted straight to the Create Session screen — and `startSession` skips the normal "mark this access code used" bookkeeping when it's the code that validated. Every real organizer-issued code (`generateAccessCode`/`AdminPanel`) is still single-use exactly as before; this is the one exception, and it never creates or touches an `opl-access-*` Supabase record at all.
- **Auth is PIN-based, not account-based.** Scorer and Organizer/admin roles are gated by hardcoded PINs in `src/lib/constants.js` (`SCORER_PIN`, `ADMIN_PIN`), checked entirely client-side. Supabase row-level security does not enforce these — see `backend/README.md` for the trust model and what a real fix looks like.
- **Matchmaking is a pluggable Strategy (Strategy pattern), not hardcoded logic.** `src/engines/RotationEngine.js` defines the interface (`generateMatchups({ waitingIds, players, existingMatchups })` → new matchup objects); `src/engines/BalancedRotationEngine.js` is the main concrete implementation (always teams a beginner with an intermediate, avoids repeat partners/opponents by recency, greedy with randomized-restart tie-breaking — see the file's doc comments for the full priority order). `src/lib/utils.js`'s `getRotationEngine(rotationMode)` maps `state.rotationMode` to an engine instance, and `refreshNextMatchups`/`regenerateNextMatchups` are the only things that call it; swapping in a different mode later (King of the Court, DUPR Rotation, ...) means adding a new `RotationEngine` subclass and a case in `getRotationEngine`, not touching the app around it.
- **Matchups are built continuously, not in synchronized rounds.** Courts run independently and pick up a new match whenever they're free — there's no "everyone finishes round 3, then round 4 starts" lockstep. `refreshNextMatchups` runs after every state change and is deliberately *strict*: it only forms a matchup from a genuine 2-beginner + 2-intermediate group, never a same-skill fallback, because players check in one at a time and an eager fallback would permanently lock two beginners together moments before an intermediate arrives (matchups are immutable once built — only `reassignMatchup`/`substituteInMatchup`/the scorer's manual edits touch them). The same-skill fallback (last resort when the beginner/intermediate counts are genuinely unequal) is opt-in, and only runs from the explicit "Regenerate matchups" control in Scorer.
- **Per-player rotation history feeds the scoring.** Each player record tracks `partnerCounts`/`recentPartnerIds`, `opponentCounts`/`lastOpponentIds`/`recentOpponentIds`, and `courtCounts`/`lastCourt`, updated by `recordRotationHistory` when a match ends (see `PickleballOpenPlay.jsx`'s `endMatch`). `state.matchHistory` logs one record per completed match.
- **Session Settings dialog — edit session properties after the session starts.** A gear icon on Scorer's Session Information card opens `SessionSettingsDialog.jsx` (new: an actual modal overlay, `styles.dialogOverlay`/`dialogCard` — the first of its kind in this app, previously everything was inline panels). Editable: venue name (validated non-empty), expected games per player, and — only when Progressive Skill Rotation is the active mode — its two phase-boundary percentages. All edits are local draft state until "Save", which calls a single new `updateSessionSettings(updates)` in `PickleballOpenPlay.jsx` (`save({ ...state, ...updates })`, one write) so Cancel/closing the dialog discards everything cleanly. **Rotation Mode is deliberately excluded** — shown read-only inside the dialog for reference — preserving the earlier decision that it's chosen once at Create Session and can't drift mid-session (see above). Court count is also left out, since Scorer's existing +/- stepper already owns that.
- **Expected Games per Player, captured at Create Session.** `CreateSessionScreen.jsx` has a numeric field (step 4, default 6, clamped to ≥1) that's passed through `startSession` into `initial.expectedGamesPerPlayer` when the session is first written to Supabase. This is the same `state.expectedGamesPerPlayer` field Progressive Skill Rotation's phase calc (`calculateSessionProgress`/`progressiveSkillPhaseFor`) already reads and that `ProgressiveSkillPanel` already lets an organizer edit mid-session (Scorer, Settings panel) — Create Session just seeds its starting value regardless of which rotation mode is chosen, rather than everyone starting from the hardcoded default of 6. No other behavior currently reads it outside that existing Progressive Skill Rotation usage.
- **Session Type Architecture — Open Play vs. Tournament (architecture only, no tournament logic yet).** `state.sessionType` (`"openPlay"` | `"tournament"`, see `SESSION_TYPES` in `lib/constants.js`) is chosen on `CreateSessionScreen.jsx` (step 3: 1 Session name, 2 Number of courts, 3 Session type, 4 Rotation Strategy *or* Tournament Format, 5 Expected games per player — Open Play only, 6 Register players) via a two-button toggle, same pattern as `SkillToggle`. It gates step 4 and step 5: **Open Play** → the existing Rotation Strategy `<select>` (`ROTATION_MODES`, unchanged logic) *and* the Expected Games per Player field; **Tournament** → both of those are hidden, replaced by a Tournament Format `<select>` (`state.tournamentFormat`, see `TOURNAMENT_FORMATS` — Round Robin / Single Elimination / Double Elimination — the last two explicitly marked as placeholders in the UI copy). Selecting a Tournament session does not currently change any runtime behavior — Scorer, the rotation engines, and everything else still key off `rotationMode` alone, which continues to default to `"continuous"` regardless of `sessionType`; a Tournament session today is functionally identical to an Open Play one.
  - **Naming decision, worth knowing:** the stored/internal field is still `rotationMode`, not `rotationStrategy` — "Rotation Strategy" is the UI label and conceptual grouping only. Every rotation engine (`getRotationEngine`, `refreshNextMatchups`/`regenerateNextMatchups`, `isPoolingRotation`, `progressiveSkillPhaseFor`), `ScorerView.jsx`, `PickleballOpenPlay.jsx`, and `RotationSimulationEngine.js` all still read/write `rotationMode` untouched. A literal field rename was considered and deliberately rejected: it would touch every one of those call sites for a purely cosmetic change, and — more importantly — old Supabase session records only have `rotationMode`, so a rename would either need a compatibility shim or silently lose old sessions' rotation setting on load. `sessionType` and `tournamentFormat` are genuinely new fields (old records simply lack them, which is harmless since nothing yet reads `sessionType` downstream of Create Session) — `rotationMode` is not.
- **Player Database — a reusable, cross-session player registry (architecture only; no statistics/rankings/tournament logic yet).** `src/lib/playerDatabase.js` stores one Supabase KV record per player (`opl-player-{id}`, shared, listed by prefix — `PLAYER_DB_PREFIX` in `lib/constants.js`), the same one-record-per-entity pattern sessions and access codes already use, deliberately not a single aggregate blob (which would need a full read-modify-write on every add and risks two organizers clobbering each other). A record: `id, firstName, lastName (nullable), displayName, photo, gender (nullable), skill, duprRating (nullable), contactNumber (nullable), notes (nullable), active, createdAt, updatedAt`.
  - **The identity link that makes this a real "single source of truth":** a *session's* per-player record (`state.players[id]` in `PickleballOpenPlay.jsx` — games/wins/losses/checkedIn/skipped, reset fresh every session) is completely unchanged in shape and stays that way. What changed is where its `id` comes from: `CreateSessionScreen.jsx` now reuses the Player Database record's own id as the roster entry's id, whether that player was picked from the database or just created. That means a future Statistics/Rankings/Season-History feature can join a player's results across every session they've ever played purely by id — no migration, no name-matching heuristics, because the id has been consistent since the record was first created.
  - **Create Session's registration step (step 6)** now offers **Select existing player** (a search box, active players only, styled like the existing `PlayerPicker` search pattern — already-added players show "Added" and can't be re-added) and **Create new player** (First Name required, Last Name/Display Name/Skill/Photo as the primary fields, with Gender/DUPR rating/Contact number/Notes tucked into a collapsible "More details" section so the fast path stays fast). Creating a player always saves it to the database *and* adds it to the current roster in one action — this is also what satisfies "auto-create records if no database exists yet": there's no seed/migration script, the database simply starts empty and grows from the first player anyone creates. A save failure doesn't block the organizer — the player still joins the local roster, with a warning surfaced — since a background persistence failure shouldn't stop someone from starting their session.
  - **Deliberately out of scope:** Check-in's walk-in add (`quickAddCheckIn` in `PickleballOpenPlay.jsx`) is untouched — walk-ins remain session-only, not saved to the database. There's also no player-management screen yet (editing an existing record, or setting `active: false`) — `active` defaults to `true` and nothing currently changes it. Both are natural follow-up tasks, not done here to keep this one architectural change scoped.
- **Tournament Engine Foundation — the shared architecture every tournament format plugs into (architecture only; no elimination/standings/seeding/champion logic yet).** Built on top of the Round Robin Scheduler below, extending its data model and formalizing a Strategy-pattern interface for format engines to share, the same role `RotationEngine` plays for Open Play's rotation strategies:
  - **Extended data model** (`src/lib/tournamentModel.js`, additive fields — old stored tournament records simply lack them, harmless since nothing reads them yet): `Tournament` gains `name` and `status` (`draft`|`ready`|`running`|`completed` — a freshly generated tournament starts `"ready"` since it already has its full schedule; nothing transitions it further yet, no live match tracking exists). `TournamentRound` gains `status` (mirrors match status) and `courtAssignments` (the round's court numbers, redundant with but surfaced alongside `matches[].court`). `TournamentMatch` gains `winner` and `score`, both `null` until match scoring is actually built. The former "entrant" is now formally the **Tournament Participant** model — `makeParticipant(label, playerIds, seed)` → `{ id, label, playerIds, seed, status }`; `makeEntrant` (the name the working scheduler already imports) is kept as an alias so nothing existing needed to change.
  - **`src/engines/TournamentEngine.js`** — the abstract base class: `generateSchedule()`, `updateMatchResult()`, `getStandings()`, `getNextMatches()`, each throwing by default, exactly mirroring `RotationEngine`'s own single-method version of this pattern. `RoundRobinEngine`/`SingleEliminationEngine`/`DoubleEliminationEngine` (all three in `src/engines/`) extend it.
  - **`RoundRobinEngine.generateSchedule()` was the only real method at the time this foundation shipped** — it delegates straight to the already-working `RoundRobinScheduler` (see below). `updateMatchResult()` became real too in the Tournament Match Management task below; `getStandings()`/`getNextMatches()` and all four methods on Single/Double Elimination still return an inert `{ implemented: false, message }` placeholder rather than throwing, so a future caller exploring the interface doesn't crash on formats/methods that aren't built yet.
  - **`getTournamentEngine(format)`** (`src/lib/tournament.js`) — a format → engine instance registry, additive and not wired into the existing `buildAndSaveRoundRobinTournament` call path (which still calls `RoundRobinScheduler` directly, deliberately untouched so the shipped flow can't regress). `saveMatchResult`/`saveMatchStart` (see Tournament Match Management below) *do* go through it.
  - **Tournament Dashboard UI**: the session nav's tournament-only tab is now labeled **"Tournament"** (was "Schedule") and renders `TournamentDashboardView.jsx` — five sub-tabs: Overview, Participants, Schedule, Standings, Bracket. At the time this foundation shipped only **Schedule** was real; **Overview** became real too in Tournament Match Management (see below). Participants/Standings/Bracket remain static "Coming soon" placeholder panels with no data or logic.
- **Tournament Match Management — recording and progressing Round Robin match results (no standings/rankings/champion/tie-breaker logic yet).** Makes `RoundRobinEngine.updateMatchResult()` real and wires match actions into the Schedule tab, building directly on the Tournament Engine Foundation above:
  - **Data model refinement** (`lib/tournamentModel.js`): `TournamentMatch.score` changes from a bare `null` to `{ teamA: number|null, teamB: number|null }`; adds `completedAt` (ms epoch, set when a result is saved). Reads of `score` treat both the old bare-`null` shape and the new object shape defensively (`m.score?.teamA`), so tournaments generated before this task still render without erroring. New format-agnostic helpers — `findMatch`, `computeRoundStatus`, `computeTournamentStatus`, `getTournamentProgress` (Total/Completed/Remaining/Percent, counting only non-bye matches), and `startMatch` (pending → inProgress) — live here rather than on `TournamentEngine`, since "locate a match" and "roll a round/tournament's status up from its matches" aren't scheduling/results logic a format needs to customize; every format's engine can reuse them as-is.
  - **`RoundRobinEngine.updateMatchResult(tournament, matchId, { scoreA, scoreB, winnerId })`**: validates (both scores present and ≥0, a winner chosen, the tournament not already `completed`) before writing anything, throwing a message-bearing `Error` the UI surfaces directly to the organizer. On success it writes the match's score/winner/`status: "completed"`/`completedAt`, then re-derives that round's status and the whole tournament's status via the shared helpers above — first result saved flips the tournament from `"ready"` to `"running"`; the last remaining real match flips it to `"completed"`, after which further edits are rejected (both by this validation and by the UI hiding "Edit result" once `tournament.status === "completed"`).
  - **`saveMatchStart`/`saveMatchResult`** (`lib/tournament.js`) — thin orchestration: call the model/engine functions above, then persist via the existing `saveTournament`. Neither touches Open Play session state (`window.storage.set` on the session's own key) at all — match results are a mutation of the independent Tournament record only.
  - **Ownership move for live progress**: the tournament fetch moved from `TournamentScheduleView` up to `TournamentDashboardView`, which now owns one live copy passed to both the Overview tab (renders `getTournamentProgress` as a stat row + a fill-bar, `styles.tournamentProgressTrack`/`tournamentProgressFill`) and the Schedule tab. A result saved in Schedule calls `onTournamentChange` up to the Dashboard, so Overview's numbers update immediately without a second fetch or a stale copy — this is what makes "Automatic Updates: refresh the schedule/update progress" true instead of just eventually-true-on-remount.
- **Round Robin Standings — a live, modular standings table (no champion determination, tie-breakers beyond the default sort, or Double Elimination logic).** Standings are pure derived data: nothing new is persisted on the Tournament record, and nothing incrementally patches a cached total — a caller just recomputes from `tournament.rounds` on every render. That's what makes it "live" with zero new plumbing: the Dashboard already holds one up-to-date `tournament` copy (see Tournament Match Management above) that updates immediately after every match save, so Standings and Overview are simply re-derived on the next render, no manual refresh trigger needed anywhere.
  - **`src/engines/TournamentStandingsService.js`** — abstract base, same Strategy-pattern role as `TournamentEngine.js` one level up: `calculateStandings(tournament)`, `updateAfterMatch(tournament)`, `sortStandings(rows, comparator)`. Kept as its own service rather than folded into `TournamentEngine` because a format's *ranking* rules are a genuinely separate concern from its scheduling and result-recording rules — a future format could reuse one without the others.
  - **`RoundRobinStandingsService.js`** — the real implementation. `calculateStandings` aggregates every **completed**, non-bye match per participant (Matches Played, Wins, Losses, Points For/Against, derived Win % and Point Differential); every entrant appears even at 0 matches played, so the table always lists the full field. `sortStandings` applies `defaultRoundRobinComparator` — Wins desc → Win % desc → Point Differential desc → Points For desc, exported as its own named function (not inlined) and accepted as an optional parameter, so a future tie-breaker task can pass a different comparator without touching `calculateStandings` at all. `updateAfterMatch` is a full recompute-and-resort rather than an incremental patch — correctness over micro-optimization, since a Round Robin tournament has at most a few dozen matches.
  - **`RoundRobinEngine.getStandings()` is now real**, delegating to this service — the same "engine method delegates to a dedicated module" precedent `generateSchedule`/`updateMatchResult` already set. Other formats' `getStandings()` remain placeholders.
  - **UI**: the Standings tab (`TournamentStandingsView.jsx`) replaces its placeholder with a real table — Rank, Player/Team, MP, W, L, Win %, PF, PA, +/- — via `getTournamentEngine(tournament.format).getStandings(tournament)`, scoped to `format === "roundRobin"` (other formats show a "not available yet" message). 🥇🥈🥉 highlight the top 3 rows, but only once at least one match has actually been decided — at 0-0 every entrant is arbitrarily tied, so a "podium" would be meaningless. The Overview tab's stat row changed to match this task's spec exactly: Total Teams / Matches Completed / Matches Remaining / Current Leader (the #1-ranked participant's name once any match is complete, else "—").
  - **Match card UI** (`TournamentScheduleView.jsx`): "Start Match" (pending) and "Edit Result" (completed, hidden once the tournament itself is `completed`) both open the same score-entry form — Team A/B score inputs, a Team A/Team B winner-select row, Save/Cancel — rather than "Start Match" and "Enter Scores" being two separate flows; fewer clicks, the same four organizer actions the task specifies. Inline validation errors render in the form itself. A completed match is visually distinguished (`styles.matchCompletedCard` — a green-tinted border/background) with the winning side's score highlighted (reusing `HistoryView`'s `historyScoreWin` styling).
- **Champion Determination — automatic finalization for a completed Round Robin tournament (no advanced tie-breakers, Single/Double Elimination, awards ceremony, or PDF export).** A tournament was already computing `status: "completed"` once every real match had a result (`computeTournamentStatus`, from Tournament Match Management) — this task adds *what happens next* once that's true, not the completion detection itself:
  - **`src/engines/TournamentCompletionService.js`** — abstract base, the same Strategy-pattern role as `TournamentStandingsService.js`/`TournamentEngine.js`: `isTournamentComplete(tournament)`, `determineChampion(tournament)`, `finalizeTournament(tournament)`. Kept as its own service since "when is this format done, and who won" is a distinct concern from scheduling, result recording, and ranking — a future format (e.g. Single Elimination, where "done" means the final match, not every match) implements this independently of the others.
  - **`RoundRobinCompletionService.js`** — the real implementation. `isTournamentComplete` reuses `getTournamentProgress` (every real match completed). `determineChampion` reuses `RoundRobinStandingsService` — champion/runner-up/third place are simply ranks 1/2/3 of the same standings the Standings tab already shows, not a separate calculation; each is `null` if the field is too small for that slot (e.g. no Third Place with only 2 entrants). `finalizeTournament` is idempotent — once `completedAt` is stamped it returns the tournament unchanged on any later call, so it's safe to call unconditionally.
  - **Automatic wiring**: `RoundRobinEngine.updateMatchResult()` already recomputes `tournament.status` via `computeTournamentStatus` after every save; this task adds one line — once that lands on `"completed"`, `RoundRobinCompletionService.finalizeTournament()` runs on the result before it's returned. Champion/runner-up/third place are stamped the moment the last match is saved, with no separate "finalize" action for the organizer to trigger. `saveMatchResult` (`lib/tournament.js`) is unchanged — it already persists whatever the engine returns.
  - **Data model** (`lib/tournamentModel.js`): `makeTournament` gains `completedAt` (ms epoch | null) and `champion`/`runnerUp`/`thirdPlace` (each `{ participantId, label } | null`), all starting `null`.
  - **Validation**: editing a completed tournament's matches or starting a new one was already rejected by `updateMatchResult`/`startMatch` (Tournament Match Management) — unchanged. New this task: regenerating the schedule is now blocked once `tournament.status === "completed"`, both in `TournamentDashboardView`'s `handleGenerate` (inline error, belt) and in `TournamentScheduleView` (the "Regenerate schedule" button itself is hidden, suspenders).
  - **UI**: the Overview tab now renders a distinct completed-state panel once `tournament.status === "completed"` — 🥇 Champion / 🥈 Runner-up / 🥉 Third Place, Tournament Status, Total Matches, Matches Completed, Completion Time — replacing the live-progress view (which is unchanged for in-progress tournaments). The Standings tab shows a "Tournament Complete" badge next to the section label once completed; standings are effectively frozen simply because no further match edits are possible, not through any separate "freeze" mechanism.
- **Round Robin Pool Support — multiple independent Round Robin pools within one tournament (no Playoff Qualification, elimination brackets, or non-random assignment methods yet).** A **breaking data-model change with no migration**: a Tournament no longer carries `entrants`/`rounds`/`champion`/`runnerUp`/`thirdPlace`/`completedAt` directly — those all moved one level down onto a new `TournamentPool` (`{ id, label, entrants, rounds, status, completedAt, champion, runnerUp, thirdPlace }`) in `tournament.pools[]`. Deliberately not migrated (unlike Rotation Mode, which stays backward-compatible on purpose) since the whole Tournament Manager epic is still unreleased/in-progress — no organizer has a real tournament record in the old shape yet.
  - **The reuse trick**: `TournamentStandingsService`/`TournamentCompletionService` (and `getTournamentProgress`'s pool-scoped sibling, `getPoolProgress`) were never actually coupled to "the whole Tournament" — they only ever read `.entrants`/`.rounds` off whatever's passed in. A `TournamentPool` has exactly that shape, so **neither service needed a single logic change** for pools to work — `RoundRobinEngine` just calls them once per pool (`standingsService.updateAfterMatch(pool)`, `completionService.finalizeTournament(pool)`) instead of once per tournament. Parameter names in both services were renamed `tournament` → `entity` to reflect this ("a Tournament or TournamentPool, either works") — a comment-and-naming change only, not a behavior change.
  - **`src/engines/PoolAssignment.js`** (new) — a small registry (`POOL_ASSIGNMENT_METHODS = { random: randomAssignment }`), same Strategy-pattern shape as `getTournamentEngine`, with `assignPools(entrants, poolCount, method)` as the seam future methods (manual, snake seeding, DUPR rating, skill level) register into without touching any caller. `distributeEvenly(n, poolCount)` implements the task's exact examples — remainder players go to the earliest pools, one each (18/3 → 6-6-6; 22/4 → 6-6-5-5). `randomAssignment` shuffles (reusing `lib/random.js`'s existing `shuffle`, not a new implementation) then slices by `distributeEvenly`'s sizes. `poolLabel(index)` produces "Pool A"/"Pool B"/… (wrapping to "Pool AA" past Z, though Custom pool counts in the millions aren't a realistic organizer input).
  - **`lib/tournamentModel.js`**: `makeTournamentPool` builds the shape above. The old single-scope `computeTournamentStatus` became `computePoolStatus(pool)` (identical logic, just explicitly one pool's rounds); a *new* `computeTournamentStatus(tournament)` derives the overall status purely from `tournament.pools.map(p => p.status)` — `completed` only once every pool is, `running` as soon as any one pool has started or finished, else `ready`. `findMatch` now searches every pool's rounds (a match id is unique tournament-wide regardless of which pool). `startMatch` locks per pool — a completed pool's matches reject further changes even while a sibling pool is still `ready`/`running`.
  - **`RoundRobinEngine.updateMatchResult(tournament, matchId, result)`**: unchanged validation logic, now scoped to the match's containing pool (found via `findMatch`) — writes the result into that pool's rounds only, rolls that pool's status up via `computeRoundStatus`/`computePoolStatus`, and — the moment that specific pool's status reaches `completed` — finalizes *that pool* (not the tournament) via `RoundRobinCompletionService`. The tournament's own `status` is then recomputed from every pool via the new `computeTournamentStatus`. `getStandings(tournament, poolId)` now requires a `poolId` — standings are never combined across pools.
  - **`lib/tournament.js`**: `buildAndSaveRoundRobinTournament` gains `poolCount`/`assignmentMethod` params — builds the flat entrant list exactly as before, splits it via `assignPools`, then builds one `TournamentPool` per group by calling the *already-existing, unchanged* `generateRoundRobinSchedule` once per pool (no new scheduling logic — "existing Round Robin logic reused for each pool" per the task spec).
  - **UI**: `TournamentScheduleView.jsx` adds a "Number of Pools" selector (1/2/3/4/Custom, reusing the existing `skillToggleBtn` style) next to the Singles/Doubles toggle; a pool's match cards lock (`poolCompleted`, renamed from the old tournament-wide `tournamentCompleted`) independently of any sibling pool. `TournamentDashboardView.jsx` owns a new `selectedPool` ('all' | poolId) piece of state, shared by the Schedule and Standings tabs so switching between them doesn't lose which pool you were looking at; a pool-tab row (`All Pools`/`Pool A`/`Pool B`/…) renders only when `pools.length > 1` — with the default `poolCount: 1`, the Dashboard looks and behaves exactly as it did before this task, no extra chrome for the common case. Overview replaces the single "Current Leader" stat with Number of Pools plus a per-pool table (Pool / Teams / Matches / Leader); once every pool completes, it shows one podium per pool (no combined "tournament champion" — pools are fully independent until a future Playoff Qualification feature exists to combine them). Standings stacks every pool's own table under "All Pools", each with its own medals and "Pool Complete" badge, plus an overall "Tournament Complete" badge next to the section label once every pool is done.
- **Playoff Qualification — automatically determines which teams advance once every pool completes, and what playoff stage that implies (no quarterfinal/semifinal/final matches, bracket generation, or winner advancement yet — this only identifies who's in and what size bracket that means).** Sits one level ABOVE pools, the cross-pool concern Round Robin Pool Support's own entry above deliberately called out as future work.
  - **`src/engines/QualificationService.js`** (new, abstract base) — `determineQualifiers(tournament, engine)`, `calculatePlayoffSize(qualifiedCount)`, `getQualifiedTeams(tournament, engine)`. The `engine` (a `TournamentEngine`, e.g. `RoundRobinEngine`) is passed in by the caller rather than looked up internally, specifically to avoid a circular import — `lib/tournament.js` owns the format→engine registry and would otherwise need to import this file while this file imported back from it.
  - **`PoolQualificationService.js`** (new, the one real implementation) — deliberately format-agnostic: it never re-derives a ranking itself, it only calls `engine.getStandings(tournament, pool.id)` (the same call the Standings tab already makes) per pool and slices the top `tournament.advancesPerPool` rows as qualified. Any future pooled format reuses this unchanged as long as its engine implements `getStandings` the same way. `calculatePlayoffSize` maps a qualifier count onto a stage label (2→Championship Match, 4→Semifinals, 8→Quarterfinals, 16→Round of 16); other counts (a Custom "advances per pool" value won't always land on a power of two) get a generic `"N-Team Playoff"` fallback rather than an error. Qualification is only meaningful once **every** pool is `completed` — `determineQualifiers` returns an explicit `{ ready: false, ... }` shape before that, rather than a misleading provisional answer.
  - **Pure derived data, no new persistence** — same architectural pattern Standings and Champion Determination already established: `determineQualifiers` recomputes straight from `tournament.pools` and each pool's already-real standings on every call. Nothing new is written to the Tournament record.
  - **Data model** (`lib/tournamentModel.js`): `makeTournament` gains `advancesPerPool` (number, default `1`) — the only new persisted field, and it's *configuration*, not a result.
  - **Validation**: "Teams Advancing Per Pool" can't exceed the smallest pool's size. Enforced twice — `TournamentScheduleView` blocks the Generate button with an inline warning (computed the same way `PoolAssignment.js`'s `distributeEvenly` would size the smallest pool: `floor(playerCount / poolCount)`), and `buildAndSaveRoundRobinTournament` throws defensively if that's somehow violated anyway (belt-and-suspenders, matching the existing "2 players per pool" pattern).
  - **UI**: `TournamentScheduleView.jsx` adds a "Teams Advancing Per Pool" selector (Top 1/2/3/4/Custom, same control style as "Number of Pools") that flows through `generateTournamentSchedule` → `buildAndSaveRoundRobinTournament` → `makeTournament`. New **Qualification** tab (`TournamentQualificationView.jsx`) — a "not ready yet" placeholder until every pool completes, then a per-pool table (Rank / Player-Team / Qualified-Eliminated tag) plus a flat "Overall Qualifiers" list and a summary card (Teams Advancing Per Pool / Qualified Teams / Playoff Stage). The completed-state Overview panel gains a "Playoff Stage" stat (e.g. "Quarterfinals (8)") alongside the existing per-pool podiums.
- **Playoff Bracket Generation — generates and displays a seeded elimination bracket from Playoff Qualification's qualified teams (structure only: no match scoring, winner advancement, automatic progression, bronze match, or Double Elimination yet).** Sits one level above Qualification the same way Qualification sits one level above pools — a genuinely separate concern (turning a flat qualifier list into a seeded bracket structure) that a future standalone elimination format could reuse without the pool-qualification half.
  - **`src/engines/playoffStages.js`** (new, shared) — `stageNameForCount(count)` (2→Championship Match, 4→Semifinals, 8→Quarterfinals, 16→Round of 16, else `Round of N`) and `exactStageName(count)` (same table, no fallback). Extracted out of `PoolQualificationService` (which had this table inline) so both it and the new bracket generator share one source of truth instead of duplicating it — `PoolQualificationService.calculatePlayoffSize` now calls `exactStageName` with its own `N-Team Playoff` fallback (a qualifier count doesn't have to be a power of two — Custom "advances per pool" values won't always land on one), while the bracket generator uses `stageNameForCount`'s `Round of N` fallback (bracket rounds are always a power of two by construction, since `buildRounds` rejects anything else).
  - **`src/engines/BracketSeeding.js`** (new) — a small method registry (mirrors `PoolAssignment.js`'s shape), with `standardCrossPoolSeeding(qualifiedTeams)` as the one real implementation and `assignSeeds(qualifiedTeams, method)` as the seam Random/Snake/Manual/DUPR-based seeding register into later. Working through the task's own worked examples revealed the "cross-pool" pairing effect (A1 vs D2, B1 vs C2, ...) falls entirely out of a bracket's round-1 mirror pairing (seed 1 vs seed N, seed 2 vs seed N-1, ...) — the seeding function itself is a plain concatenation: group qualifiers by rank tier (every pool's 1st, then every pool's 2nd, ...), sort each tier by pool label, concatenate. No reversal or snaking needed; verified against both the 2-pool and 4-pool examples exactly.
  - **`src/engines/BracketGeneratorService.js`** (new, abstract base) — `generateBracket(tournament, engine)`, `assignSeeds(qualifiedTeams, method)`, `buildRounds(seededTeams)`. Same reasoning as `QualificationService` for taking `engine` as a parameter rather than looking it up internally (avoids a circular import with `lib/tournament.js`).
  - **`SingleEliminationBracketGenerator.js`** (new, the one real implementation) — deliberately generic over any power-of-two qualifier count, not hardcoded to 2/4/8/16 ("future bracket sizes should be easy to add" falls out for free — `buildRounds` just keeps halving until it reaches 1). Reuses `PoolQualificationService` for "who qualified" rather than re-deriving it. `buildRounds` fully populates round 1 with real seeded teams (via the round-1 mirror pairing) but every later round is pre-built with the correct match count and **empty `teamA`/`teamB` slots** — there's no winner-advancement logic yet to fill them in, so they stay `null`/"TBD" by design. `generateBracket` returns `{ ready: false, reason: "not_ready" }` while pools are still in progress, or `{ ready: false, reason: "unsupported_size", size }` if the qualified count isn't a power of two (e.g. `advancesPerPool: 3` across 2 pools = 6 qualifiers) — a real, if unglamorous, possibility now that "Teams Advancing Per Pool" is configurable, and one this milestone doesn't invent bye-round logic to work around.
  - **Pure derived data, no new persistence** — same pattern as Standings/Qualification/Champion Determination: the bracket recomputes from `tournament.pools` (via `PoolQualificationService`) on every render. No new tournament fields at all this task.
  - **UI**: new **Bracket** tab (`TournamentBracketView.jsx`) replaces the placeholder — one column per round (round name, match cards), each match card showing seed number + team name (or an italicized "TBD" for an empty later-round slot), a status badge (always `pending` this milestone), and a `COURT TBD` placeholder tag (explicitly requested — no real court assignment is implemented here either). Renders a "not ready yet" message before every pool completes, or a "needs a power-of-two qualifier count" message with guidance to adjust Teams Advancing Per Pool if the qualified count doesn't fit a bracket. Adapts automatically to bracket size purely because it renders whatever rounds the generator produced — no size-specific UI logic.
- **Playoff Match Management & Winner Advancement — makes the bracket playable end-to-end, from the first elimination round to a crowned champion (no bronze match, consolation bracket, double elimination, or advanced seeding).** **Supersedes the "pure derived data" architecture Playoff Bracket Generation deliberately chose** — scoring needs stable match identities to save results onto, so the bracket now becomes real, persisted state, the same way pool matches already are. `Tournament` gains a `bracket` field (nullable): `{ id, size, seeds, rounds, status, completedAt, champion, runnerUp }` — structurally the same shape a `TournamentPool` already has, one level up. Deliberately does **not** touch `tournament.status`'s existing meaning (pool play done) — the bracket tracks the playoff phase's own lifecycle independently, exactly the way each pool already tracks its own, so nothing built in earlier tasks needed to change.
  - **Automatic generation, no button** — consistent with every other milestone in this epic (Standings/Champion Determination/Qualification were all automatic). `RoundRobinEngine.updateMatchResult()` already recomputes `tournament.status` from every pool after each save; this task adds one more step — the moment that lands on `"completed"` and `tournament.bracket` is still `null`, it asks `SingleEliminationBracketGenerator` for one and attaches the result (now the actual persistable record — `generateBracket`'s `ready: true` return grew `id`/`status: "ready"`/`completedAt: null`/`champion: null`/`runnerUp: null`) as-is. If the qualified count isn't a power of two, `bracket` simply stays `null` — the Bracket tab's existing "needs a power-of-two count" preview (still computed live via the same generator) keeps working unchanged for that case.
  - **`SingleEliminationBracketGenerator.buildRounds`** now assigns real court numbers to round 1 (cycling `1..courtsCount`, the same convention `RoundRobinScheduler` uses — court was a `null` placeholder in the prior milestone, now a real Match Management field); later rounds' matches still start with `court: null` until a winner advances into them (there's no match to assign a court to until then).
  - **New `src/engines/PlayoffEngine.js`** — operates purely on a generic `{ rounds: [...] }` bracket object with zero Round-Robin- or pool-specific knowledge (never imports anything pool-related), which is exactly what makes it reusable by a future standalone Single Elimination tournament format without modification — that format's bracket just needs the same rounds/matches shape. `startMatch(bracket, matchId)`: pending → inProgress, rejecting a bracket that's already `completed` or a match whose `teamA`/`teamB` aren't both known yet ("prevent matches from starting before participants are known"). `updateBracket(bracket, matchId, result)`: same validation shape `RoundRobinEngine.updateMatchResult` already established (scores present/non-negative, a winner chosen, not already completed) plus the teams-known check, writes the result, calls `advanceWinner`, rolls up round/bracket status, and — the instant `isTournamentComplete()` becomes true — stamps `champion`/`runnerUp`/`completedAt` and locks the bracket. `advanceWinner(bracket, matchId, winnerTeam)`: writes the winner into the next round's match via `matchNumber → ceil(matchNumber/2)` (odd → `teamA`, even → `teamB`) — a generic, structurally-consistent bracket-tree mapping, not a seed-optimal placement (Advanced Seeding stays out of scope). A next-round match's slots start `null` and only fill in this way — that null-until-advanced state **is** "the round is locked," no separate flag to keep in sync. `isRoundComplete(round)`/`isTournamentComplete(bracket)`: pure predicates (every match in a round completed; the final round's match completed).
  - **`lib/tournament.js`** gains `savePlayoffMatchStart`/`savePlayoffMatchResult`, the exact same "call the engine, persist what it returns" shape `saveMatchStart`/`saveMatchResult` already use for pool matches, just routed through `PlayoffEngine` and `tournament.bracket` instead of `RoundRobinEngine`/`tournament.pools`.
  - **UI**: `TournamentBracketView.jsx` now branches on whether `tournament.bracket` exists. If it does, every match card gets the real Start Match / Enter Scores / Save Result flow (mirroring the pool `MatchCard`, reimplemented here rather than shared since bracket cards have an extra "TBD — waiting on a previous round" locked state pool matches never need); completed matches get the existing `matchCompletedCard` styling with the winning score highlighted; once `bracket.status === "completed"`, a 🥇 Champion / 🥈 Runner-up banner replaces the size/seeding hint and no match keeps an Edit Result action (the bracket is locked). If `tournament.bracket` is still `null`, the view falls back to the prior milestone's live preview unchanged. `TournamentDashboardView` gains `handlePlayoffStartMatch`/`handlePlayoffSaveResult` following the identical "call the lib function, `setTournament(updated)`" pattern the pool handlers already use — this is what makes "refresh the bracket immediately" free, no new plumbing. The completed-state Overview panel gains a Bracket Champion/Runner-up stat once the bracket finishes, alongside the existing per-pool podiums and Playoff Stage line.
- **Tournament Court Assignment & Match Queue — live, organizer-driven court management across pool play and playoffs (no automatic court optimization, estimated match duration, referee assignment, or notifications).** **A deliberate behavior change to already-shipped scheduling**: `RoundRobinScheduler.generateRoundRobinSchedule` and `SingleEliminationBracketGenerator.buildRounds` no longer pre-assign a court number to any match (including round 1, which previously cycled `1..courtsCount` — see Round Robin Scheduler and Playoff Match Management above). Every match now generates with `court: null`; the Schedule and Bracket tabs' "COURT N" badges read "COURT TBD" until a court is explicitly assigned here. Nothing about starting, scoring, or advancing matches changed — organizers who never open the Courts tab can still run every match exactly as before, just without a court label.
  - **Court occupancy is derived, not a separately persisted field** — the same "pure derived data" pattern Standings/Qualification/the bracket preview already use. A court is "occupied" exactly when some non-completed match has `match.court === court.number`; there's no `currentMatchId` to keep in sync. This is what makes "free the court when a match finishes" and "refresh the Court Board automatically" cost **zero new code**: freeing happens as a side effect of `RoundRobinEngine`/`PlayoffEngine`'s existing, unmodified status transitions, and the board is simply recomputed fresh from current tournament state on every render.
  - **Data model** (`lib/tournamentModel.js`): `Tournament` gains `courts: Court[]` (`{ id, number, name, status: "available" | "maintenance" }`), seeded 1..`courtsCount` at generation and independently editable afterward (rename, add, remove, mark under maintenance).
  - **New `src/engines/CourtAssignmentService.js`** — a single concrete class, not a Strategy-pattern hierarchy like the Standings/Qualification/Bracket services (the spec asks for one reusable implementation, not per-format ones). Generic by construction instead: it walks `tournament.pools` (if present) and `tournament.bracket` (if present) for matches, wherever a format happens to keep them — already covers Round Robin and Round Robin + Playoffs, and a future standalone Single/Double Elimination format would naturally reuse the same bracket-shaped record `PlayoffEngine` already works with, so this walk covers those too without changes later. `getPlayableMatches(tournament)`: pending matches with both teams known and `court === null` — this *is* the queue; a bracket match's teams staying `null` until `PlayoffEngine.advanceWinner` fills them in already satisfies "feeder matches complete" for free, no separate check needed. `getAvailableCourts(tournament)`: not in maintenance, not occupied (derived). `assignMatchToCourt`/`releaseCourt`: set/clear `match.court` (and revert `inProgress` → `pending` on release) — deliberately independent of match status otherwise, so assigning a court and starting a match stay two separate organizer actions, not fused into one. `refreshQueue(tournament)`: the pure aggregate (courts enriched with derived status + current match, plus the queue) the Court Board renders directly.
  - **`lib/tournament.js`** gains `saveCourtAssignment`/`saveCourtRelease`/`saveAddCourt`/`saveRemoveCourt`/`saveSetCourtStatus`/`saveRenameCourt` (the last four are simple `tournament.courts` metadata edits, handled inline rather than as three more one-line `CourtAssignmentService` methods beyond the five the architecture calls for) plus `saveCourtReassignment`, which performs release-then-assign on the **same in-memory tournament** before the single `saveTournament` call — not two separate `saveCourtRelease`/`saveCourtAssignment` calls in sequence, which would race (the second call's `tournament` argument would still be the pre-release version until the caller's own state re-renders in between).
  - **UI**: new **Courts** tab (`TournamentCourtsView.jsx`) — a Court Board (name, derived status, current match + status, and the next queued match as "up next," no time estimate) with per-court Start Match / Release / Reassign actions, an Add Court control, and an Available/Maintenance toggle; a Match Queue list with a per-row court picker. `TournamentDashboardView` gains the matching `handleAssignMatch`/`handleReleaseCourt`/`handleReassignMatch`/`handleAddCourt`/`handleRemoveCourt`/`handleSetCourtStatus` handlers, the same "call the lib function, `setTournament(updated)`" pattern every other tab's handlers already use.
- **Tournament Display Mode ("TV Mode") — a read-only, full-screen spectator display optimized for TVs/projectors/large monitors (no QR codes, public internet sharing, remote scoring, or player check-in).** Read-only by construction rather than by a guard: the new view never imports a single `save*`/mutate function, so there's nothing to disable — none of the Start Match/score-entry/court-assignment machinery exists in this component at all.
  - **Reached two ways** — a "TV Display" header button (the `Tv` icon, tournament sessions only) switches the *current* browser into Display Mode using the session already in memory, or a **`?display=CODE` URL query param**, read once on mount, which is the only way a *second* device (the actual TV) can land directly in Display Mode without going through Create/Join at all. The app has no router anywhere else (`PickleballOpenPlay.jsx`'s `screen` state is plain `useState`); this one query-param read is a deliberate, narrow exception — still entirely local (same deployed instance, no new sharing infrastructure, no QR code, nothing public), just how a browser already pointed at a TV gets to a specific session.
  - **True automatic refresh, not just "recompute on next render"** — `TournamentDashboardView` (the organizer's own view) fetches the tournament once and only updates its local copy after the organizer's *own* actions; it was never wired to realtime updates from other devices, since it never needed to be. Display Mode has no local actions to trigger anything, so `TournamentDisplayView.jsx` independently fetches and `window.storage.subscribeToKey`s both the session record and the tournament record (the same realtime mechanism `PickleballOpenPlay.jsx` already uses for its own session) — any device's score entry, winner advancement, court assignment, or standings change reaches the display within about a second, with no polling loop and no manual refresh control anywhere on the page.
  - **Fully independent of the organizer dashboard's data flow** — `TournamentDisplayView` takes only a `sessionCode` prop and does its own `window.storage.get`/`subscribeToKey` calls; it doesn't read or depend on `PickleballOpenPlay`'s `state`/`sessionCode`/`load()` at all, which is what makes the direct-URL entry path work (there's no organizer session loaded in memory when a TV opens `?display=CODE` cold).
  - **Reuses existing engines purely for reads** — `CourtAssignmentService.refreshQueue` for the Live Court Board and Next Matches queue, `getTournamentEngine(...).getStandings` for pool standings, `tournament.bracket` directly for the live elimination bracket (winners appear automatically since it's the same subscribed, live-refreshing data everything else on the page uses — nothing display-specific needed for that). Which section shows — Standings or Bracket — switches automatically on `tournament.bracket`'s existence, no manual toggle: no bracket yet means pool play is still the active phase.
  - **New `src/displayStyles.js`** — a deliberately separate stylesheet from `styles.js`, not a themed variant of it: the organizer UI is tuned for a phone/laptop held close-up (compact, dense, small type), while a TV is viewed from across a room, so this uses much larger type, higher contrast, a fixed dark background (not light/dark-mode aware, a deliberate permanent choice for this one screen), and fluid `clamp()`/CSS-grid sizing rather than fixed breakpoints — verified at 1920×1080 and landscape-tablet (1280×800) resolutions.
  - **Bug found and fixed during verification**: `index.css`'s global `#root { max-width: 900px }` (tuned for the organizer's phone/laptop screens) was silently capping Display Mode to a narrow centered column even at 1920×1080. Fixed by giving the display screen `position: fixed; inset: 0` — a fixed-position element sizes against the viewport, not against its constrained ancestor — rather than touching the global rule, which must stay in place for every other screen.
  - **Future compatibility (architectural note, not built this task)**: the component takes `tournament`/`session` as its data source; extending it to Open Play sessions or Single/Double Elimination later is a matter of branching on `sessionType`/`format` at the top, not a redesign — not implemented now since the "Display Pages" spec this task shipped is written entirely in Round-Robin-tournament terms (courts/standings/brackets).
- **Tournament Templates — reusable saved tournament configurations, eliminating repetitive Create Session setup (no cloud sharing, import/export, or organization-wide templates).** Templates span two screens that today hold tournament config at different times: `CreateSessionScreen` (courts, format — set before a session exists) and `TournamentScheduleView` (Singles/Doubles, pool count, assignment method, qualifiers — only meaningful once schedule generation happens, well after session creation). A template pre-fills both: selecting one at Create Session immediately updates Number of Courts/Tournament Format there, and the rest of the config rides along on the new session as `state.pendingTournamentTemplate` (a new nullable field, `null` for every Open Play session and for "Start From Scratch" tournaments) — `TournamentScheduleView` reads it once as its initial Singles/Doubles/pool-count/qualifier defaults, falling back to the old hardcoded defaults when there's no pending template. Since Generate Schedule is what actually starts a Round Robin tournament, this satisfies "pre-fill while still editable before the tournament starts" exactly — nothing is locked in until the organizer clicks Generate.
  - **New `src/engines/TournamentTemplateService.js`** — one KV record per **custom** template (`opl-template-{id}`, shared — the same one-record-per-entity pattern `lib/playerDatabase.js` already uses), plus a tiny singleton key (`opl-template-default`) holding just the default template's id. **Built-in templates are NOT stored records** — `BUILT_IN_TEMPLATES` is a static in-memory array (stable string ids, not `uid()`, so re-running the module never mints new ones) always merged into `fetchAllTemplates()`'s result. This means the 5 presets (Club Ladder Night, Weekend Round Robin, Round Robin + Top 2 Playoffs, 16-Team Single Elimination, Beginner Social Tournament) always exist with zero seeding/migration, and can never be silently edited or corrupted — `saveTemplate`/`deleteTemplate` both explicitly reject built-in ids, and duplicating one produces a fresh, independently-editable custom copy instead.
  - **`saveTemplate()` validates** (non-empty name, `courtsCount`/`poolCount`/`advancesPerPool` each ≥ 1) and throws with a message rather than silently clamping, the same pattern every other save-path in this app already uses. **`applyTemplate(template, { tournament })` structurally enforces "never overwrites an existing tournament"**: it throws if a non-null `tournament` is passed, rather than relying on call-site discipline to never pass one — every current call site only ever calls it during Create Session (before any tournament exists), and this makes that a guarantee, not just a convention.
  - **Forward-compatible by design, not by assumption**: `format` is stored as a free string, not restricted to `roundRobin` — a template shape doesn't need to change for a future Single/Double Elimination format, whose own apply path would simply ignore the pool-specific fields (`poolCount`/`assignmentMethod`/`advancesPerPool`) it has no use for.
  - **Match Scoring Rules is captured, not enforced**: `{ matchFormat, winningScore, winByTwo }` (reshaped by the later Tournament Settings task — see below — from this task's original `{ pointsToWin, winBy, bestOf }`) is stored on the template and (if the tournament was created from one) on the tournament record itself, shown as an informational line on the Overview tab — nothing in this app validates or enforces scoring rules anywhere, so this is a placeholder for a future feature, the same way `seed`/`status` on a Tournament Participant were introduced as placeholders long before anything read them.
  - **Default Court Names** apply 1:1 by index when `buildAndSaveRoundRobinTournament`/`makeTournament` seed `tournament.courts` (`makeCourt(i + 1, courtNames?.[i])`) — any courts beyond the template's name list still fall back to the usual "Court N".
  - **UI**: a new `screen: "templates"` (reached via a "Manage tournament templates" link on the Landing screen, alongside the existing "Manage access codes"/"Developer" links) — `TournamentTemplatesScreen.jsx` lists every template with Create/Edit/Duplicate/Delete/Set Default actions (Edit and Delete hidden for built-ins; Set Default available on all) and a form covering every Template Field. `CreateSessionScreen.jsx` gains a new "Create Tournament: Start From Scratch / Use Template" step (tournament sessions only, defaulting to whichever template is marked default if any exist) with a live "Pre-fills…" summary line that updates as the selection changes.
- **Tournament Settings — a consolidated place to view/edit tournament configuration after generation, without the heavy-handed "Regenerate schedule" the Schedule tab otherwise requires for any change (no new tournament formats, no new seeding algorithms).** **Deliberately not a data-migration**: every setting still lives exactly where it always has, directly on the top-level Tournament record — `RoundRobinEngine`, `PoolQualificationService`, `CourtAssignmentService`, `SingleEliminationBracketGenerator`, `TournamentScheduleView`, and `TournamentTemplateService` all keep reading/writing exactly what they already did, zero risk to any of that already-shipped code. `TournamentSettings.js` (`deriveSettingsView`) is a consolidated **read** shape over those existing fields; `TournamentRulesService.js` owns lock-state, validation, and writing changes back.
  - **Two genuinely new fields**: `playoffEnabled` (bool, default `true`) and `autoDetectPlayoffStage`/`manualPlayoffStage` (bool + enum, default auto/`null`) — neither existed before this task.
  - **`matchScoringRules` reshaped** from Tournament Templates' original `{ pointsToWin, winBy, bestOf }` to this task's exact fields — `{ matchFormat: "oneGame" | "bestOf3" | "bestOf5", winningScore, winByTwo }` — updated everywhere that shape is read or written (`TournamentDashboardView`'s Overview line, `TournamentTemplateService`'s built-ins/`makeTemplate` default, `TournamentTemplatesScreen`'s form). Safe to reshape since nothing enforces it either way — no data-loss risk, just a naming/structure change on a field introduced one task earlier.
  - **What's captured vs. real behavior**: Match Format/Winning Score/Win by Two and Auto-detect/Manual Playoff Stage are captured settings only (displayed, not enforced — the exact precedent `matchScoringRules` already set; building real multi-game scoring or a manual bracket-size override would mean new scoring/seeding logic, explicitly out of scope). **`playoffEnabled` is the one exception** — a one-line gate on `RoundRobinEngine`'s existing auto-generate-bracket step (`next.playoffEnabled !== false`, defaulting `true` so every tournament created before this field existed keeps behaving exactly as before): set to "No" and the bracket genuinely never gets generated once pool play finishes.
  - **`TournamentRulesService.getPhase(tournament)`** returns `'notStarted' | 'poolPlayStarted' | 'playoffsStarted'`, checking match-level status directly (`pool.status !== "ready"` / any bracket match `status !== "pending"`) rather than trusting the aggregate `pool.status`/`bracket.status` fields as a shortcut. **Bug found and fixed during this task's own verification**: `PlayoffEngine.startMatch` updates the match itself but never rolls `bracket.status` up to `"running"` (only `PlayoffEngine.updateBracket`, on a saved result, does) — relying on `bracket.status` alone would have missed "playoffs started" until the *first result* was saved, not the moment a match actually starts. Walking matches directly sidesteps that gap instead of depending on it being fixed elsewhere in `PlayoffEngine`.
  - **Editing Rules implemented exactly as spec'd, plus one deliberate tightening**: `notStarted` — everything editable. `poolPlayStarted` — locks Tournament Format/Number of Pools/Pool Assignment Method (the three named in the spec); everything else (mode, court names, advances-per-pool, playoff settings, match rules) stays editable. `playoffsStarted` — locks everything structural (adds mode/advances-per-pool/playoff settings/match rules to the locked set); Tournament Name and Court Names are never locked at any phase (pure cosmetic — "display preferences"). **Number of Courts is tightened beyond the spec's literal minimum**: locked from `poolPlayStarted` onward rather than staying open through pool play, since resizing courts while a match might be mid-play on the court being removed risks orphaning that assignment — the Courts tab's already-tested `saveAddCourt`/`saveRemoveCourt` remain the supported way to change court count once play has begun; `TournamentRulesService.updateSettings` deliberately never touches `tournament.courts.length` itself, keeping exactly one code path that creates/removes a Court record.
  - **UI**: new **Settings** tab on the Tournament Dashboard — General/Court Names/Pool/Playoff/Match Rules sections, each locked field showing a lock icon plus a phase-level explanation banner at the top ("Pool play has started — Tournament Format, Number of Pools, and Pool Assignment Method are locked."); a single "Save changes" button diffs the draft against the live tournament and only sends actually-changed keys through `saveTournamentSettings`, so an untouched locked field never trips the lock check on save.
- **Tournament Reports & Export Center — a centralized Reports tab for viewing, printing, and exporting tournament data (no email sharing, cloud storage, Excel export, or scheduled reports).** Pure derivation, no new storage: same "read-view, not a data migration" precedent `TournamentSettings.deriveSettingsView` set — every report is recomputed fresh from `tournament.pools`/`bracket`/`courts` on every call, nothing new is persisted. Format-agnostic by the same means `CourtAssignmentService.collectMatches` already established — it walks `tournament.pools` (if present) and `tournament.bracket` (if present), so a future standalone Single/Double Elimination format needs no changes here either.
  - **`src/engines/TournamentReportService.js`** — a single concrete class (same "one reusable service, not per-format" precedent as `CourtAssignmentService`/`TournamentRulesService`). `generateTournamentSummary`/`generateStandingsReport`/`generateMatchReport`/`generateCourtUtilizationReport`/`generatePoolReport` each return a generic `{ title, columns, rows: string[][] }` shape (`generatePoolReport` returns one of these per pool, since a multi-pool tournament has no single combined pool report). Champion/Runner-up/Third Place prefer the bracket's result (`tournament.bracket.champion/runnerUp` once `bracket.status === "completed"`) over a single pool's own podium, since the bracket is the tournament-wide result once playoffs exist; Third Place reads "—" when there's no bracket (or a multi-pool tournament with no bracket, where pools are independent and there's no single tournament-wide result at all — see `TournamentDashboardView`'s `PoolPodium`) since no bronze-medal match exists yet (see FEATURES.md's Bronze Match backlog item). Court Utilization's "Average Match Duration" is an explicit "—" placeholder, per the spec's own allowance — only `completedAt` is tracked per match, not a start timestamp, so there's no duration to compute (same gap `HistoryView.jsx`'s Game History already documented and left out for the same reason).
  - **`src/engines/ExportService.js`** — deliberately knows nothing about tournaments, only about the generic `{ title, columns, rows }` shape, which is what leaves room for an Excel export method to be added later without touching any report generator, and what makes it reusable by Open Play's own `HistoryView.jsx` later if a future task wants that (this task doesn't rebuild a duplicate Reports UI for Open Play — it already has its own bespoke CSV/JSON export). `exportCSV` mirrors `HistoryView.jsx`'s `exportHistoryCsv` exactly (`Blob` → object URL → anchor `.click()` → `URL.revokeObjectURL`), not a shared import, since `HistoryView`'s already-shipped copy is left untouched. `exportPDF` is browser-native print-to-PDF (`window.print()`) rather than a PDF-generation library — none is installed in this project, and adding one would be a first-of-its-kind new dependency for a task whose spec explicitly excludes Excel export; the organizer chooses "Save as PDF" in the OS print dialog instead.
  - **UI**: new **Reports** tab (`TournamentReportsView.jsx`) on the Tournament Dashboard, with a report-type sub-nav (Tournament Summary/Standings/Match Results/Court Usage/Pools) and Print/Export PDF/Export CSV actions. Print and Export PDF both call `ExportService.exportPDF()` — the spec lists them as separate UI buttons, but they're the same underlying browser action. A `@media print` rule scoped to `#tournament-report-print-area` hides the rest of the page (dashboard tabs, report-type nav, export buttons) so printing only ever produces the current report table, not the whole dashboard chrome around it.
- **Player Portal — read-only tournament lookups for participants, no organizer access required (no authentication, push notifications, messaging, or chat).** Reached two ways, mirroring Tournament Display Mode's precedent exactly: a "Player Portal? Look up your matches →" landing-page link (asks for a session code, since a player has no organizer session already loaded) or a `?portal=CODE` URL param read once on mount (for an organizer-shared direct link). Strictly read-only by construction, not by a guard — `PlayerPortalService`/`PlayerTournamentView`/`PlayerPortalScreen` never import a single `save*`/mutate function, same precedent `TournamentDisplayView` already set. Both the session and tournament records are realtime-subscribed (`window.storage.subscribeToKey`, same mechanism Display Mode uses), so Assigned Court/Match Status stay current with no manual refresh.
  - **Player Number is a new concept invented for this task's MVP search, not a real ID.** There is no numbering scheme anywhere else in this app (no real Player Login/authentication exists — explicitly out of scope here too), so `listAllParticipants(tournament)` derives one: a non-persisted, 1-based index across every pool's entrants in pool order. It's shown next to each name in search results (not something a player has to already know ahead of time) — a real limitation worth naming: it's only genuinely useful once an organizer separately communicates numbers (e.g. printed on a bracket sheet), which this task doesn't build tooling for.
  - **`src/engines/PlayerPortalService.js`** — pure derivation (same "read-view, not a data migration" precedent as `TournamentSettings`/`TournamentReportService`): `searchParticipants` (name substring or exact Player Number match; empty query returns nobody, a deliberate "search first, browse never" shape for a public player-facing lookup), `getParticipantSummary` (Tournament Name/Event/Current Status/Current Pool/Current Rank — Current Status reuses `TournamentDisplayView`'s `currentStage` phase language so a player sees the same wording the TV display shows), `getParticipantMatches` (completed vs. upcoming, walking `tournament.pools`/`bracket` the same format-agnostic way `CourtAssignmentService.collectMatches`/`TournamentReportService`'s traversal already do), `getParticipantResults` (Wins/Losses/Point Differential/Standing, straight off the participant's own pool standings row via `RoundRobinStandingsService` — can never drift from what the organizer's own Standings tab shows), `getParticipantBracketPath` (`null` until a bracket exists; otherwise every bracket match this participant appears in, in round order, plus `nextOpponent` read off the first unfinished one — "TBD" if the feeder match hasn't produced a winner yet, a real name once it has).
  - **`src/components/PlayerTournamentView.jsx`** — renders the four read-only sections (My Tournament/My Matches/My Results/Live Bracket) from already-derived `PlayerPortalService` output; Live Bracket only renders when `getParticipantBracketPath` returns non-null.
  - **`src/components/PlayerPortalScreen.jsx`** — the code-entry + search shell: resolves a session code to its tournament, then a live-filtered search box (`PlayerPortalService.searchParticipants`, no explicit Search button — the list re-filters on every keystroke) lets the player pick themselves from the results before `PlayerTournamentView` renders.
  - **`src/engines/PlayerNotificationService.js` is architecture only, per the spec's explicit "do not implement push notifications."** Exports `NOTIFICATION_TYPES` (`COURT_ASSIGNMENT`/`MATCH_READY`/`TOURNAMENT_COMPLETE`) and a `getNotifications()` that always returns `[]` — nothing calls it from the UI (no bell icon, no polling), the same "captured, not enforced" precedent `matchScoringRules`/`autoDetectPlayoffStage` already set elsewhere, settling the notification *shape* ahead of a future task that actually implements delivery.
- **Pickleball King Branding — a rebrand of every user-facing brand string, no business/tournament logic or database schema changes (task named the product "Pickleball King"; the organizer's own instruction at approval time asked for "PickleKing" instead — used throughout the actual app, keeping the FEATURES.md item name matching the task's literal wording).** New `src/lib/brand.js` (`APP_NAME`, `TAGLINE`, `FOOTER_TEXT`) is the single source of truth every component imports from instead of hardcoding the strings — this alone is what "architecture allows white-label editions/custom logos/club branding without changing application logic" asks for: a future white-label variant swaps this one file (plus a logo asset), no component touches its own brand text directly. Color theming already had its own seam before this task — every color in `styles.js` reads from CSS custom properties (`--color-*`), not hardcoded values — so a future dark-mode or club-branded palette swaps those variables, not component code; this task didn't need to add anything there, just confirm it already existed.
  - **Two unavoidable literal duplicates**: `index.html`'s `<title>`/`apple-mobile-web-app-title` and `vite.config.js`'s PWA manifest `name`/`short_name`/`description` can't import `src/lib/brand.js` (a static HTML file, and a build-time config file outside the app bundle, respectively) — both are commented "keep in sync with brand.js" at the point of duplication.
  - **Updated**: browser title, landing page title/tagline, in-app header kicker, a new footer line ("Powered by CONNECT.PH") added to both the landing screen and the existing in-session footer, `README.md`/`CONTRIBUTING.md` opening text, `package.json`'s `description` field, and the two comment strings in `supabase/schema.sql` (comment text only, not the table/column definitions — doesn't touch the schema itself, per the spec's explicit exclusion).
  - **Deliberately left unchanged**: `package.json`'s npm `name` field (`"openplay-manager"`) — an internal package/tooling identifier, not itself user-facing, riskier to rename without confirming it doesn't break deploy config; the "ORMOC CITY, LEYTE" landing kicker and the "Ormoc City Saturday Open Play" venue-name placeholders in Create Session/Session Settings — these describe the operating club's own location/example session name, not the product's brand, and aren't in the spec's replacement list; any visual/color redesign — the spec asked for "a modern sports-inspired identity" but this task scoped to text/metadata rebranding plus confirming the architecture already supports theming, not a new visual design; and an icon audit — lucide-react (the app's only icon library) has no pickleball-specific icon, so no in-app icon swap was made rather than forcing a poor substitute.
  - **No dedicated About page or route exists in this single-page app** (no router anywhere in `PickleballOpenPlay.jsx`'s `screen` state machine) — the spec's "About page" bullet is satisfied by the landing hero's tagline and the new footer, not a new page.
- **Role-Based Access Control — a reusable permission framework (Roles/Permissions/Permission Groups/AuthorizationService), no billing, multi-club logic, OAuth/SSO, or 2FA.** **The central scoping fact**: this app has no user-account/login system at all — only two hardcoded PINs (`ADMIN_PIN` gates the Organizer panel, `SCORER_PIN` gates Scorer within a session), so there's no real "current user" anywhere to check permissions against everywhere. Building one would be authentication, which this task explicitly excludes. So the framework itself is fully real and unit-tested, but the one live enforcement point wired into the running app is the new User Management screen's own actions — reached only after the existing Admin PIN, which is treated as this app's one real "acting as an administrator" boundary (a `ACTING_ADMIN` object with the built-in Super Admin role, in `UserManagementScreen.jsx`). Full action-level enforcement across every existing feature (Scorer PIN, tournament actions, etc.) needs real user sessions — a natural follow-up once a real login task exists (see FEATURES.md's already-tracked, unchecked "Player Login").
  - **`src/lib/permissions.js`** — the permission catalog: `PERMISSION_GROUPS` (Tournament/Players/Courts/Reports/Settings/Administration) and a flat `PERMISSIONS` list, each permission tagged with a group id. Static data, not stored records — same "built-in, always in memory" precedent `BUILT_IN_TEMPLATES` already set — there's nothing here to create or edit, only Roles pick from it. Every permission id traces directly back to a bullet in the spec's per-role Can/Cannot lists (commented inline), rather than being invented independently.
  - **`src/engines/Role.js`** — `makeRole`/`BUILT_IN_ROLES`, the exact "built-in vs. custom" precedent `TournamentTemplateService`/`BUILT_IN_TEMPLATES` already established: the 6 named roles are static, immutable, in-memory objects; a **custom role** (this task's architecture seam for the unchecked "Custom Roles" FEATURES.md item, not implemented as a feature yet) would be a real `opl-role-{id}` KV record via `fetchAllRoles`/`saveRole`, following the identical fetch-merge-with-built-ins shape `TournamentTemplateService.fetchAllTemplates` uses. Every spec'd exclusion (Tournament Director "Cannot: Delete the club / Manage billing"; Referee "Cannot edit tournament settings") is enforced simply by that permission id never appearing in the role's `permissionIds` — no separate deny-rule list needed.
  - **`src/engines/AuthorizationService.js`** — the one class every protected action is meant to check through: `hasPermission(user, permissionId, customRoles)` (a disabled user has zero permissions regardless of role, checked once here rather than at every call site), `canAccess(user, groupId, customRoles)` (true if any assigned role grants any permission in that group — for UI section-gating, not a specific action), `assignRole`/`removeRole` (add/remove a role id from `user.roleIds`, immutably). **`roleIds` is an array, not a single role** — a user can hold more than one role at once (e.g. Referee + Staff) with zero extra machinery, since every check just unions permission ids across every assigned role; verified this works correctly (a Referee-only user can't check in players, but gains that permission the moment Staff is also assigned, and loses it again on `removeRole`). `customRoles` defaults to `[]` everywhere, so every existing call site works unchanged once real custom roles exist — they just start passing a non-empty array.
  - **`src/lib/userDatabase.js`** — one KV record per user (`opl-user-{id}`, shared), the exact same one-record-per-entity pattern `lib/playerDatabase.js` already uses — deliberately a separate table from the Player Database: a User here is someone with administrative/staff access to the app, not a pickleball player's reusable identity, even though the same real person could be both. `clubId` (nullable, free string) exists on every user record now — the seam a future Multi-Club Management feature reads/writes — but nothing anywhere branches on more than one club existing; it's a placeholder field, not multi-club logic, per the spec's explicit exclusion.
  - **UI**: new **User Management** screen (`UserManagementScreen.jsx`), reached via a "Manage users →" link on the existing Admin Panel. Table of Name/Role(s)/Club/Status; Add/Edit User (name, free-text club, a multi-select role list — any number of the 6 built-in roles); Disable User is a soft `status: "disabled"` flip (same precedent as the Player Database's `active` flag), not a delete; Reset Password is an explicit placeholder button (shows "Password reset isn't implemented yet.", auto-clears) since there's no credential store anywhere yet to reset anything against.
- **League Management — multi-week league scheduling/scoring/standings across skill divisions, reusing the Tournament Manager's engines wherever possible (no promotions/relegation, playoffs, team leagues, or payments).** **The central reuse decision**: a `LeagueSeason` is built via the *existing* `makeTournament()` factory (`format: "league"`, `playoffEnabled: false` so `RoundRobinEngine`'s bracket auto-generation step never fires — a clean way to hard-exclude playoffs rather than just leaving them unbuilt) with league-only fields layered on top (`leagueId`, `season`, `startDate`, `endDate`, `matchDay`, `matchTime`). Each division is a `TournamentPool`, reused as-is — `RoundRobinStandingsService`/`RoundRobinEngine`/`CourtAssignmentService`/`TournamentReportService` all already only read `.entrants`/`.rounds`/`.pools`/`.bracket`/`.courts`, never a format discriminant, so a division's schedule/scoring/standings/reports all work with **zero new match-management code** — the same reuse-by-duck-typing this task's spec explicitly asked for. Stored under its own prefixes (`opl-league-`, `opl-league-season-`, see `lib/constants.js`) so League Seasons don't show up in the Tournament Manager's own list, even though they're internally Tournament-shaped.
  - **`src/lib/leagueModel.js`**: `League` (`{id, name, clubId}` — a recurring club-level container; one League can run many Seasons over time, e.g. "Tuesday Night League" → Fall 2026 → Winter 2027), `LeagueSeason` (the Tournament-shaped record above), `LeagueWeek` (`makeLeagueWeek` — exactly the existing `TournamentRound` shape via `computeRoundStatus`, plus one added field, `matchDate`), `LeagueMatch` (a documented alias of `makeMatch`, the same "same shape, different domain name" precedent `makeEntrant` already is for `makeParticipant`). `computeWeekDate(startDate, matchDay, weekNumber)` maps a weekday name + season start to the actual calendar date of week N (first occurrence of that weekday on/after `startDate`, then +7 days per week) — verified against a known Sunday start landing correctly on the following Tuesday, 7 days apart per week.
  - **Divisions are configurable for free**: "Beginner/Intermediate/Advanced/Open" (`SKILL_DIVISIONS` in `lib/constants.js`) are just suggested default labels for the New Season form — a division is really only a `TournamentPool` with a `label`, so any custom division name already works with zero extra machinery, satisfying "future divisions should be configurable" without a separate catalog to maintain.
  - **`src/lib/league.js`** (orchestration, mirrors `lib/tournament.js` almost line for line): `buildAndSaveLeagueSeason` (per division: `pairIntoTeams`/`generateRoundRobinSchedule`, both reused unmodified from `RoundRobinScheduler.js` — bye handling for odd player counts falls out for free, exactly like a Tournament pool already gets it), `saveLeagueMatchStart`/`saveLeagueMatchResult` (reusing `tournamentModel.js`'s `startMatch` and `RoundRobinEngine.updateMatchResult` directly against the League Season object), `saveLeagueCourtAssignment`/`saveLeagueCourtRelease` (reusing `CourtAssignmentService` directly). **Manual edits** are scoped narrowly per the plan: `swapWeekMatches` exchanges the two teams between two *not-yet-started* matches in the same week/division (rejects a bye, or either match already started/completed, so a real result can never be silently overwritten) — this is the one matchup-editing capability built beyond court/score edits (already covered above), not a full rescheduler.
  - **`src/engines/LeagueStandingsService.js`** — delegates every existing stat (Wins/Losses/Games Won/Games Lost/Points For/Points Against/Point Differential — the spec's own naming for exactly what `RoundRobinStandingsService` already computes as `pointsFor`/`pointsAgainst`/`pointDiff`) straight to `RoundRobinStandingsService`, adding only the one genuinely new stat, **League Points** — a documented default formula (2 points per win, 0 per loss) since the spec tracks League Points without defining how they're earned; adjustable later. `getPlayerRankings(season)` flattens every division's standings into one list tagged by division name, sorted by League Points then Win % — divisions stay independent skill brackets (each row keeps its own division-relative rank), this is a ranked view across them, not a re-ranking algorithm.
  - **Season Dashboard** (`LeagueSeasonDashboardView.jsx`): Overview (Current Week — the earliest week across any division still incomplete, else the season's last week — Upcoming/Completed Matches with inline Start/Enter Score), Standings (one table per division), Player Rankings (the flattened cross-division list above), and a Reports tab that calls `TournamentReportService`/`ExportService` **directly** on the League Season object — zero new report code, since those services already only need the same `.pools`/`.bracket`/`.courts` shape every Tournament report already reads.
  - **Setup UI** (`LeagueManagerScreen.jsx`): create a League, then a Season under it (name/season/start-end dates/match day/match time/courts/Singles-Doubles/divisions), sourcing each division's roster from the **Player Database** (`fetchAllPlayers`) via a checkbox picker — Leagues aren't tied to an Open Play session the way tournaments are, so the Player Database (already built, previously only used by Create Session's picker) is the natural, already-existing roster source rather than inventing a new one.
  - **Scoped out of the UI on purpose**: no separate Courts tab (court number is a field folded into the Weeks/Overview match rows instead); no drag-and-drop rescheduler (see `swapWeekMatches` above).
- **Membership Management — member profiles, plans, status, and eligibility, reusing the Player model wherever possible (no payment gateways, billing, invoicing, or email reminders).** **The central reuse decision**: the spec's "Extend the existing Player profile" is taken literally — every membership field lives directly on the existing `lib/playerDatabase.js` record (additive, no migration, same as every other field this app has ever added to a stored record), not a separate member table. Two fields the spec asks for already existed and are reused as-is rather than duplicated: `skill` → Skill Level, `notes` → Notes. Only the genuinely new fields were added: `memberId`, `membershipStatus`, `membershipPlanId`, `joinDate`, `expirationDate`, `emergencyContact`, `duprId` (a placeholder account-id string, distinct from the pre-existing `duprRating` number).
  - **`MembershipPlan`** (`src/lib/membershipPlans.js`) — the third use in this codebase of the same built-in/custom precedent (`TournamentTemplateService`, RBAC's `Role.js`): Daily Pass/Monthly/Quarterly/Annual/Lifetime ship as static built-ins (each a `durationDays`, `null` for Lifetime — "never expires"); a custom plan is a real `opl-membership-plan-{id}` record. This is what satisfies "future plans should be configurable" for free.
  - **Membership Status is derived-with-override, not a dumb stored field** — the same "don't trust a stale stored value, recompute the live truth" pattern this app already uses for tournament/court status. `MembershipService.getMembershipStatus(player)`: a manually-set `"suspended"` always wins (an organizer's explicit override, checked first); an `expirationDate` in the past always reads as `"expired"` even if the stored status still says `"active"` (verified — nobody has to remember to manually expire people); no plan assigned yet reads as `"pending"`; otherwise the stored status. `Membership` itself (`deriveMembership(player, customPlans)`) is a read-only derived view over these fields plus the resolved `MembershipPlan` object, the same "read-view, not a data migration" precedent `TournamentSettings`/`PlayerPortalService` already established — nothing new is persisted by deriving it.
  - **`src/engines/MembershipService.js`** — `isMemberActive`/`getMembershipStatus` (above), `renewMembership(player, planId, customPlans)` (pure function, caller persists — extends `expirationDate` from the LATER of today or the player's current still-active expiration, so renewing early adds time rather than discarding it; a Lifetime plan clears `expirationDate` entirely; `joinDate` is set once, on a player's first-ever renewal, and never overwritten by a later one — all verified), `validateEligibility(player, requirements)` where `requirements = { requireActiveMembership, requiredPlanId, allowGuests }` — Guest Access Allowed is a full bypass when on, regardless of the other two flags.
  - **Eligibility wiring — a scoping call, same spirit as RBAC's**: `defaultEligibilityRequirements()` (`engines/TournamentSettings.js`) is captured and editable on both a Tournament (new "Membership Eligibility" section on the Tournament Settings tab, never locked — doesn't affect schedule generation) and a `LeagueSeason` (`leagueModel.js`), but **live enforcement is wired into exactly one place**: League Manager's division player picker (`LeagueManagerScreen.jsx`'s `DivisionEditor`), which calls `MembershipService.validateEligibility` before allowing a player to be added and refuses with the service's own reason string on failure — verified against an expired member (blocked), a suspended member (blocked), and an active member (allowed). Tournament Manager's roster flow is left at "captured, not enforced," the same precedent `matchScoringRules` already set, rather than touching `CreateSessionScreen`'s player flow too.
  - **UI**: new **Membership Management** screen (`MembershipScreen.jsx`, reached from a new landing-page link), two tabs. **Dashboard**: Total/Active/Expiring Soon (within 30 days)/Expired/New Members This Month, all computed live from the Player Database via `MembershipService` — verified against a seeded mix of active/expired/suspended/pending members, including a player whose *stored* status was still `"active"` past its expiration date, correctly counted as Expired. **Member Directory**: search (reuses `filterPlayersByQuery`), filter by status, sort (name/expiration/status), View Profile (every spec'd field), and **Renew Membership** — a real `MembershipService.renewMembership` call (so the Architecture method isn't a stub) with an explicit "No payment is processed here" note next to the plan picker, satisfying both "renewMembership() must be real" and "do not implement payment gateways/billing/invoicing" at once; verified end-to-end (a Pending member renewed to Active with a correctly-extended expiration date).
- **Round Robin Playoff Engine — completing the automatic Round Robin → Qualification → Bracket → Champion workflow.** **This task's central finding, established before any code was written**: the requested workflow was already almost entirely built across earlier tasks (Playoff Qualification, Bracket Generation, Playoff Match Management & Winner Advancement). Every example in this task's own spec — the 2-pool/top-2 bracket (SF1: A1 vs B2, SF2: B1 vs A2) and the 4-pool/top-2 bracket (QF: A1 vs D2, B1 vs C2, C1 vs B2, D1 vs A2) — is already verified against, near-verbatim, in `BracketSeeding.js`'s own code comments from the Bracket Generation task. Rather than create the spec's requested `PlayoffQualificationService`/`PlayoffBracketService`/`PlayoffAdvancementService` as fresh files (which would just be copies of already-shipped code, directly violating this same spec's own "avoid duplicating existing scheduling logic" instruction), the mapping is documented here instead:
  - **`PlayoffQualificationService`** → `engines/PoolQualificationService.js` (`determineQualifiers`, respects the existing Round Robin tie-break rules via `RoundRobinStandingsService`, tags each row `qualified: true/false` for the Qualification tab's Qualified/Eliminated display).
  - **`PlayoffBracketService`** → `engines/SingleEliminationBracketGenerator.js` + `engines/BracketSeeding.js` (`standardCrossPoolSeeding` — the cross-pool pairing effect the spec's examples describe falls entirely out of a plain seed-tier concatenation feeding a standard 1-vs-N bracket-mirror pairing, not any special-cased logic).
  - **`PlayoffAdvancementService`** → `engines/PlayoffEngine.js` (`advanceWinner`/`updateBracket` — winner advancement, unlocking the next match, locking the bracket and stamping Champion/Runner-up the moment the Final completes; the completed-state Overview panel in `TournamentDashboardView.jsx` is what "Update Tournament Overview" already refers to).
  - **The one genuine gap found and built this task**: nothing previously let an organizer correct a mistaken score after the tournament locked — `PlayoffEngine.reopenBracket(bracket)` unlocks a completed bracket (clears the `"completed"` lock, `completedAt`, `champion`, `runnerUp`, rolling status back to `"running"`), making every match's result editable again via the same `updateBracket`/`startMatch` gates (which only ever checked `bracket.status`, never an individual match's own status). **Deliberately scoped, not a full audit trail or cascade-repair system**: reopening does NOT retroactively re-run an already-recorded later round if an earlier feeder match's winner is corrected — `advanceWinner` only ever fills a next match's empty slot once, so correcting an early round after later rounds were already played won't propagate forward. This limitation is surfaced directly in the Bracket tab's UI copy rather than silently glossed over. New `saveReopenBracket` in `lib/tournament.js` (same "call the engine, persist what it returns" shape as every other `save*` function there) and a "Reopen tournament" button on `TournamentBracketView.jsx`'s completed-bracket state, reusing the Tournament Dashboard's existing organizer-only trust boundary rather than adding a new PIN/role gate.
  - **Not implemented, per spec**: Double Elimination, Bronze Medal Match, Consolation Bracket, Manual Seeding — all already tracked, unchecked, in FEATURES.md from earlier tasks; nothing new needed there.
- **Court Assignment & Match Queue Engine — the central engine controlling court utilization for Tournament and League (no AI optimization, referee scheduling, live notification delivery, or court reservations).** Composes rather than duplicates the existing `CourtAssignmentService` (built two tasks ago for manual assignment only) with three new engines, adding the one capability that didn't exist before: automatic assignment.
  - **`src/engines/CourtPriorityService.js`** — `calculatePriority(entry)`: Championship Match → 1 (highest), Semifinals → 2, Quarterfinals (and any earlier/unnamed bracket round, e.g. "Round of 16") → 3, pool matches → 4. `getWaitingSince(entry, tournament)`: "longest waiting first" derived entirely from existing fields, no new storage — a pool match's wait clock starts at `tournament.createdAt` (every pool match becomes eligible together, at schedule generation); a bracket match's starts at the later of its two feeder matches' `completedAt` (round 1 falls back to the latest pool `completedAt`, since qualification rather than a feeder match is what unlocks it).
  - **`src/engines/CourtQueueService.js`** — `getQueue(tournament)`: every eligible match (both participants known, court unassigned, not manually delayed), sorted by `CourtPriorityService`, enriched with Queue Position/Match Type/Priority/Estimated Wait. `getNextMatch(tournament, { forCourtNumber })`: the single highest-priority match, optionally restricted to one a specific court is allowed to take (respects a pin — see below). `estimateWaitTime()`: a documented, clearly-labeled **simple estimate** (`⌈queuePosition ÷ available courts⌉ × ASSUMED_MATCH_DURATION_MINUTES`, currently 20) — not data-driven, since this app has never tracked real match duration (the same gap already flagged in Tournament Reports' Court Utilization and Game History's Match Duration placeholders). `match.startedAt` (new this task, see below) means a future task could finally compute a real average from completed-match history; this one doesn't, on purpose, rather than fake precision.
  - **`src/engines/CourtAssignmentEngine.js`** — the orchestrator. `assignCourt`/`releaseCourt` delegate straight to `CourtAssignmentService`, unmodified. **`autoAssign(tournament, courtNumber)` is the genuinely new capability**: "when a court becomes available, automatically assign the highest-priority eligible match" — verified it correctly skips an already-occupied court (no-op) and respects a match pinned to a *different* court. **A real behavior change to already-shipped flows, made deliberately (confirmed with the organizer before implementing)**: `lib/tournament.js`'s `saveMatchResult`/`savePlayoffMatchResult` now call `autoAssign` on the court a just-completed match frees, and `saveCourtRelease` now calls the new `releaseAndAutoFill` — court assignment is no longer 100% manual by default. `lib/league.js`'s `saveLeagueMatchResult`/`saveLeagueCourtRelease` get the identical wiring, since a `LeagueSeason` is Tournament-shaped and reuses the same engine for free. New manual-override methods: `swapCourts` (exchange two occupied courts' matches, both writes on the same in-memory tournament before the one save — same anti-race precedent `saveCourtReassignment` already set), `delayMatch`/`undelayMatch` (an additive `match.queueOverride.delayed` flag — deprioritizes out of the auto-assignment queue without touching status/court; still fully visible in the Match Queue UI), `pinMatchToCourt`/`unpinMatch` (an additive `match.queueOverride.pinnedCourt` — a preference for `autoAssign`, not an immediate assignment).
  - **Two additive, backward-compatible match/court fields**: `match.startedAt` (stamped by `startMatch` in both `tournamentModel.js` and `PlayoffEngine.js` — finally enables "Time Running"/"Estimated Finish" on the Live Court Board; `null` for any match started before this field existed, which just reads as no elapsed time) and `match.queueOverride` (`{ pinnedCourt, delayed }`, absent by default — zero migration, read with safe defaults everywhere it matters). Court `status` gains `'disabled'` alongside the existing `'available'`/`'maintenance'` — `CourtAssignmentService` treats both the same for occupancy/availability (never assignable); only the UI/labeling distinguishes "temporarily under maintenance" from "permanently out of rotation."
  - **`src/engines/CourtNotificationService.js` is architecture only**, mirroring `PlayerNotificationService.js`'s exact precedent: `COURT_NOTIFICATION_TYPES` (`COURT_READY`/`PLAYERS_CALLED`/`MATCH_DELAYED`) and a `getNotifications()` that always returns `[]` — nothing calls it from the UI.
  - **Live Court Board** (`TournamentCourtsView.jsx`): Court/Current Match/Next Match/Status/Time Running/Estimated Finish per court, plus the enriched Match Queue (position/type/priority/wait) and Swap/Delay/Pin controls alongside the existing Reassign.
  - **Open Play is deliberately NOT rewired onto this engine.** Every method here is format-agnostic over the same `{ courts, pools, bracket }` shape `CourtAssignmentService` already established (a Tournament or League season, interchangeably) — but Open Play's own court/rotation system (`state.courts` holding teams directly, no separate match records with round numbers, its own `BalancedRotationEngine`/etc.) is a structurally different shape, stable and heavily tested throughout this project's history. Migrating it onto this engine would be a large, risky rewrite this task doesn't require; the seam a future Open Play adapter would need is exactly this `{ courts, pools, bracket }` interface, already proven twice (Tournament, League).
- **Pool Qualification Engine — extends Playoff Qualification's existing `PoolQualificationService` from whole-tournament to PER-POOL finalization (no bracket generation of any kind — that stays exactly where the earlier Bracket Generation task left it).** **The real gap this task found and fixed**: `determineQualifiers` used to early-return a blanket "not ready" until `tournament.status === "completed"` — i.e. ALL pools done — so a multi-pool tournament showed nothing at all for a pool that had already finished while a sibling pool was still mid-match. Now every pool's own rows finalize the moment THAT pool completes (`pool.status === "completed"`, reusing the existing `computePoolStatus` field, never re-derived), tagged `"pending"` whenever the pool itself isn't done yet — verified with a two-pool tournament where Pool A (fully played) shows real 🟢 Qualified/🔴 Eliminated while Pool B (1 of 6 matches played) shows ⏳ Pending for every row, simultaneously, on the same screen. The aggregate `qualifiedTeams`/`playoffSize`/`ready` fields keep their pre-existing contract (still gated on every pool being done) since `SingleEliminationBracketGenerator.generateBracket` already depends on that meaning — a cross-pool bracket seed list isn't meaningful until every pool's numbers are final anyway.
  - **New named methods, exactly as spec'd, none duplicating ranking logic**: `isPoolComplete(pool)` (thin wrapper over `pool.status`), `getQualificationStatus(pool, standings, qualifiersPerPool)` (the three-state tagging described above — `standings` always comes from `engine.getStandings`, ranking/tie-breaks never re-implemented here), `getQualifiedParticipants`/`getEliminatedParticipants` (flatten `determineQualifiers`' per-pool rows by status — real-time correct even mid-tournament, verified returning Pool A's qualifiers/eliminated before Pool B had thrown a single result).
  - **`validateQualifiers(pools, qualifiersPerPool, playoffEnabled)`** consolidates the two validation rules the spec calls out (qualifiers can't exceed the smallest pool; at least one qualifier required when playoffs are enabled) into one reusable check — `buildAndSaveRoundRobinTournament` (`lib/tournament.js`) now calls this instead of re-deriving the smallest-pool check inline the way it used to.
  - **The "Custom (disabled placeholder)" phrasing in this task's spec was NOT implemented literally** — `TournamentScheduleView.jsx`'s Custom qualifiers-per-pool option is already a fully working numeric input, built in an earlier task, not a placeholder. Disabling already-shipped, working functionality to match a spec's aspirational phrasing would be a real regression for no benefit; this was flagged to the organizer and confirmed before implementing, and nothing here touches that control.
  - **UI**: `TournamentQualificationView.jsx` now always renders every pool's table (previously hidden entirely behind a blanket "not ready yet" placeholder), each row showing 🟢/🔴/⏳ per that pool's own state; the Overall Qualifiers/Playoff Stage summary still only appears once `ready` is true. The Overview panel (`TournamentDashboardView.jsx`) gained Pools Completed/Pools Remaining/Qualified/Eliminated tiles, computed the same way. `styles.qualificationTag` was extended from a boolean (qualified/eliminated) to a three-state status string, with a bare-boolean fallback so no other existing call site needed touching.
- **Automatic Playoff Bracket Generator — extends `SingleEliminationBracketGenerator` (already this app's `PlayoffBracketGenerator`, `makeBracketMatch` already its `PlayoffMatch`, its round objects already its `PlayoffRound`, from the earlier Bracket Generation task) rather than creating parallel duplicate files, per this spec's own "reuse... existing Match model where appropriate."** This task's own seeding examples (2-pool/top-2: SF1 A1 vs B2, SF2 B1 vs A2; 4-pool/top-2: QF1 A1 vs D2, QF2 B1 vs C2, QF3 C1 vs B2, QF4 D1 vs A2) were already implemented and verified against, near-verbatim, by `BracketSeeding.js`'s `standardCrossPoolSeeding` from that earlier task — re-verified this task with fresh tests built from the real engine (not hand-seeded fixtures) to confirm both examples still produce the exact labeled pairings. Playoff Stage Detection (2→Final, 4→Semifinals, 8→Quarterfinals, 16→Round of 16) is `playoffStages.js`'s pre-existing `stageNameForCount`, also unchanged.
  - **Two new methods added for real, filling the one genuine gap**: `validateBracket(tournament, engine)` — every rule in the spec's "Bracket Validation" section as one callable, testable method returning `{ valid, errors[] }` (all failing rules, not just the first) rather than scattered implicit side effects: all pools complete, qualification finalized, correct (power-of-two) qualifier count, no duplicate participants among qualifiers, bracket not already generated (checked first and short-circuits, since nothing else is worth reporting once a bracket already exists). `getBracketStructure(bracket)` — a clean, flattened read-shape (round/match counts, team labels with `"TBD"` for an unfilled future-round slot, champion/runnerUp) over an already-generated bracket; returns `null` for "no bracket yet" rather than throwing, since that's an expected, normal state this whole feature is built around.
  - **Deliberately NOT wired into `generateBracket()`'s own internals** — `generateBracket` is the hot path `RoundRobinEngine.updateMatchResult` calls after every single match result, with an already-tested `{ready, reason, size}` return shape `TournamentBracketView`'s preview messaging depends on; `validateBracket` covers the same two core checks plus the two new ones as a separate, richer result for a caller that wants the full picture, without risking `generateBracket`'s existing contract. This means the same two rules are intentionally expressed twice (documented inline, not an oversight).
  - **Tournament Overview gained the 3 missing stats**: Total Qualified Participants, Total Playoff Matches (post-generation: the real match count across every round of `tournament.bracket`; pre-generation: `qualifiedCount - 1`, the standard single-elimination total), and Bracket Status (`tournament.bracket?.status` or `"Not generated"`) — "Playoff Stage" already existed from the earlier task.
  - **Not implemented, per spec** (all already tracked, unchecked, in FEATURES.md): winner advancement, automatic progression, score entry, champion determination — none of that changed; Bronze Medal Match, Double Elimination, Consolation Brackets, and Manual Seeding remain future-compatibility seams, not built.
- **Winner Advancement Engine — extends `PlayoffEngine` (already this app's `PlayoffAdvancementService`) rather than creating a parallel file, same reuse principle as the previous two tasks.** The core advancement logic (`advanceWinner`, `updateBracket`'s result-writing/round-rollup/tournament-completion/locking, `isRoundComplete`/`isTournamentComplete`) was already built and working from the Playoff Match Management task — this task found and closed two real gaps instead: no first-class concept of "Locked vs. Ready" match state (both silently read as the same `"pending"` string, so the UI had to re-infer "locked" ad hoc by checking `teamA`/`teamB`), and no explicit, callable validation/progress methods matching the spec's architecture list.
  - **`getMatchState(match)`** — the four states (`locked`/`ready`/`inProgress`/`completed`), computed fresh from existing fields every call, no new persisted field (same "derived, not stored" precedent this app follows everywhere). This is what "the mapping should come from the bracket structure rather than being hardcoded into the UI" means concretely: `TournamentBracketView.jsx`'s `BracketMatchCard` now calls this instead of locally re-deriving `locked = !match.teamA || !match.teamB`. `styles.matchStatusBadge` extended with two new status values (`locked`, `ready`) alongside the three it already handled — every existing pool/League call site keeps working unchanged since none of them pass the two new values.
  - **`getCurrentRound(bracket)`** — the earliest round with any unfinished match (falls back to the final round once everything's done, so it always returns something real). **`populateNextMatch(bracket, roundIndex, matchNumber, slot, winnerTeam)`** — the actual slot-write, pulled out of `advanceWinner` as its own named primitive per the spec's architecture list; `advanceWinner` still owns deciding WHERE a winner goes, this just performs the write once that's decided (pure decomposition, zero behavior change — verified against the full existing advancement test suite).
  - **`validateAdvancement(bracket, matchId, result)`** — every rule in the spec's "Validation" section as one real, callable method returning `{valid, errors[]}` (all failing checks at once), the same shape `validateBracket()` established for bracket generation one task ago: already-completed tournament (checked first, short-circuits), match already has a result (prevents advancing the same participant twice), missing/invalid winner, teams not yet known, and duplicate participants. **Deliberately not wired into `updateBracket()`'s own internals** — same reasoning as `validateBracket()`/`generateBracket()`: `updateBracket` is the hot path every real score save goes through, with an already-tested per-rule-throw contract `BracketMatchCard`'s local error handling depends on.
  - **A real bug caught and fixed during this task's own test-writing, before it shipped**: the first draft of the duplicate-participant check flagged every normal advancing bracket as invalid, because a winner legitimately appears twice across the bracket as a whole once they've advanced — once in their now-completed match (the historical record) and again in their new slot. Fixed by scoping the check to only currently-ACTIVE (non-completed) matches, where the real invariant lives: a participant should never be simultaneously live in two different not-yet-decided matches at once.
  - **UI**: `BracketMatchCard` now shows the real Locked/Ready/In Progress/Completed badge, and the losing side of a completed match is now visibly dimmed (50% opacity) alongside the winner's existing green highlight — both were asked for by the spec, only the winner highlight existed before. New `BracketProgressPanel` on the Bracket tab: Current Round/Matches Remaining/Completed Matches/Current Active Matches, pure derived data shown only while the bracket is in progress (the existing Champion/Runner-up banner already covers the completed case).
  - **Not implemented, per spec**: Bronze Medal Match, Double Elimination, manual bracket editing, custom seeding, ELO/player ratings — all either already tracked unchecked in FEATURES.md or (ratings) a separate, already-shipped, deliberately unrelated feature.
- **Club Rating & Ranking Engine — a persistent, club-wide player rating that updates automatically after every completed match, across Open Play/Tournament/League (no DUPR integration, AI predictions, national rankings, or cross-club ratings).** **The central identity constraint, confirmed with the organizer before implementing**: the only persistent player identity this app has is a Player Database record — Open Play session players and Tournament participants don't reliably map to one (a walk-in created fresh in a session has no Database record at all). A `PlayerRating` therefore only exists for a real Player Database id; any match participant without one is silently skipped, never errors the match-completion flow it's hooked into. This is deliberately a **third, separate** "wins/losses" concept in this app — not a replacement for Open Play's session-scoped `state.players` stats or a Tournament pool's own `RoundRobinStandingsService` numbers, both of which stay exactly as they were; `PlayerRating` is the only one that's permanent and spans every session/tournament/league a player ever plays in.
  - **`src/lib/ratingModel.js`**: `PlayerRating` (`{currentRating, previousRating, highestRating, totalMatches, wins, losses}`, one KV record per player, keyed by Player Database id — `winPct`/`trend` are deliberately NOT stored, both are one-line derivations, the same "don't persist what you can compute" precedent used everywhere in this app) and `RatingHistory` (one KV record per player holding an appended array — same "array embedded in one owner record" shape Open Play's own `matchHistory` already uses, avoiding a KV key explosion). New players default in at a **configurable** initial rating (1000 by default). **A real prefix-collision bug found and fixed during this task's own testing**: `RATING_PREFIX` ("opl-rating-") was originally a literal string-prefix of `RATING_HISTORY_PREFIX` ("opl-rating-history-"), so `window.storage.list` on one incorrectly also matched the other's keys — renamed to non-overlapping `opl-playerrating-`/`opl-ratinghistory-`.
  - **`src/engines/RatingEngine.js`** — algorithm-pluggable via a `RATING_ALGORITHMS` Strategy registry, the same shape `PoolAssignment.js`/`BracketSeeding.js`/`getRotationEngine` already use elsewhere: only `"simple"` (a fixed-K point exchange, default `K=15` — every winner gains K, every loser loses K, no probability weighting) is implemented; `elo`/`glicko2`/`duprInspired` are documented seams, not built — the point is the plug interface, not the algorithms (`duprInspired` is explicitly excluded from actually being built anyway, per "do not implement DUPR integration"). `processMatchResult({ winnerIds, loserIds, matchId, source })` is the one entry point every match-completion hook calls. **Doubles applies a uniform team-level delta to both players on a side** (not pairwise/individual-opponent Elo) — the simplest defensible default, and the reason swapping in a real Elo/Glicko-2 algorithm later means adding one function to the registry, not touching `processMatchResult` itself. `isTopRanked(playerId)` exists specifically for King Slayer, and must be called *before* `processMatchResult` updates anyone's rating — otherwise a just-defeated #1 could already have dropped out of first place by the time it's checked (verified with a test where the "was #1 going in" snapshot is taken before the rating write).
  - **`src/engines/AchievementService.js`** — First Win/10/50/100 Wins (checked against the just-updated win count, idempotent — never double-awards), Tournament Champion (awarded to every one of the champion team's underlying players the moment `PlayoffEngine`'s bracket-completion stamps a champion — reusing `resolvePlayerIds`, see below), King Slayer (awarded per-occurrence, not deduped like the others — a player could slay the king more than once over a club's history). League Champion is defined as a constant but never actually awarded — League Management has no champion/playoffs by its own earlier, deliberate scope exclusion.
  - **`src/engines/RatingLeaderboardService.js`** — Overall (by `currentRating`), Monthly/Annual (net rating change from `RatingHistory` entries within the calendar period — a reasonable read of "ranking" for a system with no separate seasonal-reset mechanism yet, which is exactly why Seasonal Resets is listed as future compatibility rather than built here), Most Improved (the same period-based net-change computation, defaulting to a rolling 30 days instead of a calendar boundary), Highest Win % (a minimum-matches floor, 3, so a 1-0 record can't misleadingly rank #1), Most Matches Played.
  - **Integration hooks, one per module, all additive**: `lib/tournament.js`'s `saveMatchResult`/`savePlayoffMatchResult` (exported `rateMatch`, reused by League rather than duplicated — a `resolvePlayerIds` helper bridges the one real shape difference: a pool match's `teamA`/`teamB` are full `Participant` objects with `playerIds` already on them, but a bracket match's are `SeededTeam` objects that only carry `participantId`/`label` — playerIds isn't threaded through bracket seeding, so bracket matches look the underlying entrant back up by `participantId` across every pool), `lib/league.js`'s `saveLeagueMatchResult` (imports and calls the exact same `rateMatch`, since a League match is a plain pool match already), and `PickleballOpenPlay.jsx`'s `endMatch` (fire-and-forget, since `endMatch` itself isn't async — a rating-side failure can never block or delay ending a live match).
  - **UI**: new `RatingsScreen.jsx` (Leaderboards tab + a Player Profile lookup — Rating History/a minimal inline-SVG Rating Chart/Recent Form/Favorite Partner/Most Frequent Opponent/achievement badges, both computed from `RatingHistory`'s `partnerId`/`opponentId` fields), reached from a new landing-page link. Read-only by construction — nothing here writes anything; ratings only ever change via the three match-completion hooks above.
- **Live Playoff Bracket & Match Operations — a full live-operations layer for playoff matches (pause/resume, walkover, court moves, click-to-inspect detail) built on top of the bracket/court/advancement engines from the four immediately preceding tasks, not a new bracket or scoring system.** Survey-first (this session's now-standard approach) found bracket rendering, court assignment, start match, and score entry/advancement already fully built; the real gaps were mid-match operational states and a UI capable of navigating and inspecting a live bracket rather than just displaying it.
  - **`src/engines/PlayoffEngine.js`** gained `pauseMatch`/`resumeMatch` (no-op unless the match is currently in the state being left, so a stale double-click can't corrupt status), `getActiveMatches`/`getCurrentRoundMatches`, and `recordWalkover(bracket, matchId, winnerId)` — the last one deliberately reuses `updateBracket({scoreA: 0, scoreB: 0, winnerId})` for real winner-advancement/tournament-completion logic rather than duplicating it, then overlays `score: {teamA: null, teamB: null}, walkover: true` onto the result. Every mutation now stamps `lastUpdatedAt` (new, additive, optional field).
  - **`src/engines/PlayoffMatchService.js`** (new) and **`src/engines/BracketViewModel.js`** (new) are thin orchestration/derivation layers matching the spec's requested architecture exactly — `PlayoffMatchService` composes `PlayoffEngine` + the existing `CourtAssignmentService` with zero duplicated logic; `BracketViewModel` is pure/never-mutates, computing `currentRoundNumber`/`waitingMatches`/`nextMatches` and, via `getAdvancementPathMatchIds`, a deliberately narrow one-round-back/one-round-forward advancement-path highlight (not a full recursive trace to the championship — matches the spec's own bounded ask).
  - **Three explicit scoping decisions**, made without a separate approval round since they were folded into the plan: "change match order" and "cancel a match" are satisfied by existing court-reassignment/release-court tools rather than new cancel semantics; "Scheduled Time (optional placeholder)" is literal static placeholder text, not a new data field.
  - **UI (`TournamentBracketView.jsx`, rewritten)**: click-to-select Match Detail panel (timestamps, walkover flag, court move control), expand/collapse per round, "Jump to current round" button, advancement-path tinted highlighting, and two new Bracket Progress stats (Waiting Matches, Next Matches) alongside the existing ones. Confirmed during browser verification that moving an in-progress match to a new court reverts its status to "Ready" — this is `CourtAssignmentService.releaseCourt()`'s pre-existing `inProgress -> pending` reversion firing as the first step of reassignment, not a bug introduced here.
  - **Not implemented, per spec**: Ratings, Memberships, League features, Double Elimination, Bronze Medal Match — all already shipped as separate features or intentionally out of scope. Referee Assignment, Live Streaming, and Push Notifications remain unchecked as explicit future compatibility.
- **Tournament Reports & History — extends the existing `TournamentReportService`/`ExportService` (built in the earlier Tournament Reports & Export Center task) rather than duplicating them, and adds the two genuinely missing pieces: a per-player statistics/playoff/timeline report set, and a searchable archive of completed tournaments.** Survey-first found `generateTournamentSummary`/`generateStandingsReport`/`generateMatchReport`/`generateCourtUtilizationReport`/`generatePoolReport` and both `exportPDF`/`exportCSV` already fully built and wired into a Reports tab; the real gaps were per-player stats, a dedicated playoff breakdown, a chronological timeline, tournament-level history/search, archival, and export validation.
  - **`TournamentReportService.js`** gained `generatePlayerStatistics()` (folds `RoundRobinStandingsService.calculateStandings()`'s pool-play numbers together with every completed bracket match a participant played, keyed by `participantId` — matches played/wins/losses/points scored/conceded now span the whole tournament, not just pool play), `generatePlayoffReport()` (bracket-only match breakdown, broken out of the generic match report), `generateTournamentTimeline()` (synthesized entirely from existing and two new additive timestamps — see below — never a persisted event log), and `assertExportable()` (throws unless `tournament.status === "completed"`, called by `TournamentReportsView` before either export button fires).
  - **Two new additive timestamps**, stamped once and never overwritten, needed because "Qualification Finalized" and "Bracket Generated" weren't previously tracked as events: `tournament.qualificationFinalizedAt` (stamped by `RoundRobinEngine.updateMatchResult` the moment pool play first completes tournament-wide) and `tournament.bracket.generatedAt` (stamped the moment `SingleEliminationBracketGenerator`'s result is attached).
  - **New `src/engines/TournamentHistoryService.js`**: `listCompletedTournaments()`/`searchTournaments()` are pure filters over whatever `fetchAllTournaments()` (new, same `fetchAll*`-then-parse pattern as `leagueModel.js`/`ratingModel.js`) returns; `archiveTournament()` is the one real mutation, requiring `status === "completed"` and setting only `archived`/`archivedAt`.
  - **Archival as a single chokepoint, not scattered guards**: `saveTournament()` in `tournamentModel.js` now throws on any write to an already-archived record unless the caller explicitly passes `{ allowArchived: true }` — since every one of the ~18 existing `save*` wrappers in `lib/tournament.js` already funnels through this one function, an archived tournament becomes read-only everywhere at once with a two-line change, not a guard added to every mutation call site. `saveArchiveTournament()` is the sole caller that passes the escape hatch.
  - **UI**: new `TournamentHistoryScreen.jsx` (search by name, filter by date range/format/archived-status, reached from a new Landing Screen link) opens a selected tournament into `TournamentHistoryDetail`, which reuses `TournamentReportsView` directly for the read-only view — that component was already pure derivation with zero mutation, so no parallel read-only Dashboard/Bracket view was built. `TournamentReportsView` gained three new tabs (Playoff Results, Player Statistics, Timeline) and an inline error banner wired to `assertExportable()`. Delete requires a two-click in-page confirm (type-to-confirm pattern used elsewhere in this app for destructive actions), and only ever targets an already-archived record.
  - **Not implemented, per spec**: Memberships, Ratings, League management, Payments, Notifications untouched; Season Reports and League Reports left unchecked as explicit future compatibility — this task's report shape (`{title, columns, rows}`, walking `tournament.pools`/`tournament.bracket`) is what a later multi-tournament rollup would aggregate across, without changing the architecture.
- **Sprint 1/Sprint 2 re-verification & `PlayoffBracketGenerator` rename — no new functionality; Pool Qualification Engine and Automatic Playoff Bracket Generation were both already fully built (this app has been ahead of a "Sprint 1 → 2 → 3" build order since the earlier Pool Qualification Engine and Automatic Playoff Bracket Generator tasks), re-verified end to end with a deliberately adversarial fixture and one cosmetic rename applied on request.** Verification ran an 8-player/2-pool tournament through the real save/engine flow (not hand-built fixtures) with a designed three-way tie at 1-2 in one pool: all three tied players were correctly separated by point differential, only the top `advancesPerPool` marked qualified, and the Qualification tab/Overview stats matched the engine output exactly. A second run with clean (non-tied) standings confirmed bracket seeding produces the exact cross-pool pairing the spec's own example describes (`SF1: A1 vs B2`, `SF2: B1 vs A2`) and that generation stops where it should — `bracket.status: "ready"`, all matches `pending`, the Final's `teamA`/`teamB` still `null` — with no premature advancement or champion.
  - **Rename only, zero behavior change**: `SingleEliminationBracketGenerator.js` → `PlayoffBracketGenerator.js` (same class, same two call sites — `RoundRobinEngine`'s auto-generation hook and `TournamentBracketView`'s preview), `assignSeeds()` → `seedParticipants()`, `buildRounds()` → `createRounds()`, `getBracketStructure()` → `getBracket()` (was already unused outside this file), purely to match a later sprint spec's requested architecture naming (`PlayoffBracketGenerator`/`generateBracket()`/`createRounds()`/`seedParticipants()`/`validateBracket()`/`getBracket()`) verbatim. Every prior PROJECT.md entry above still refers to the pre-rename name, left as an accurate historical record of what that task actually did rather than retroactively rewritten.
- **Sprint 3 — Winner Advancement Engine, re-verified and extracted into a separately named `PlayoffAdvancementService`, on the same "spec asks for exact architecture naming, behavior already correct" pattern as the Sprint 2 rename immediately above.** Automatic advancement, the four match states, the advancement mapping being bracket-derived rather than UI-hardcoded, and tournament completion/lock were all already built and working (from the earlier Winner Advancement Engine + Automatic Tournament Completion tasks) — re-verified with a fresh 4-player tournament played through pool play, one semifinal, and the championship match, confirming the winner is correctly seated into the Final's `teamA` slot, the untouched semifinal stays `Ready`, and the Final stays `Locked` until both semis finish.
  - **Real extraction, not a pure rename**: `advanceWinner()`, `populateNextMatch()`, `isRoundComplete()`, `getCurrentRound()`, and `validateAdvancement()` moved out of `PlayoffEngine.js` into new `src/engines/PlayoffAdvancementService.js` (identical logic, unchanged), which `PlayoffEngine` now composes (`const advancementService = new PlayoffAdvancementService()`) rather than duplicates — `PlayoffEngine` keeps same-named delegating wrappers for every moved method so its existing external callers (`BracketViewModel.getCurrentRound`, `TournamentBracketView`'s preview, `updateBracket`'s own internal calls) needed zero changes. A genuinely new method, `updateMatchStatus(match, status)`, was added to `PlayoffAdvancementService` as the one real status-setter (validates against the four stored values — Locked/Ready are never persisted, only derived by `getMatchState`) and wired into `startMatch`/`pauseMatch`/`resumeMatch`/`updateBracket`'s completed-transition in place of each one's previous inline `{ ...m, status: "x" }` literal, so it's an actually-used method, not decorative API-shape padding.
  - **`PlayoffBracketGenerator` (bracket generation) was not touched**, per the spec's explicit "do not modify bracket generation logic" — this task only ever imports/composes it, never edits `PlayoffBracketGenerator.js`.
- **Sprint 6 — Bronze Medal Match, a genuinely new feature (unlike the previous three sprints, which turned out to already be built).** Optional per-tournament 3rd/4th-place match, integrated into `PlayoffBracketGenerator`/`PlayoffAdvancementService`/`PlayoffEngine`/`CourtAssignmentService`/`TournamentReportService` with zero changes to any of their existing behavior when disabled (the default).
  - **Core design decision: `bracket.bronzeMatch` is a sibling field, not a round inside `bracket.rounds`.** The spec's own diagram shows it sitting between Semifinals and Final, but inserting it into the `rounds` array would break the `roundIndex + 1` adjacency `PlayoffAdvancementService.advanceWinner`/`populateNextMatch` rely on for every other round — those two methods are **completely untouched** by this task. `PlayoffBracketGenerator.generateBracket()` attaches `bracket.bronzeMatch` (+ `thirdPlace: null`/`fourthPlace: null`) only when `tournament.bronzeMatchEnabled` is true **and** there's an actual semifinal round (`rounds.length >= 2` — a 2-team bracket has none, so bronze is silently skipped, not an error, verified with a dedicated test).
  - **`PlayoffAdvancementService`** gained one new method, `populateBronzeMatch(bronzeMatch, semifinalMatchNumber, loserTeam)` — same odd-matchNumber→teamA/even→teamB convention `advanceWinner` already uses, since there are always exactly two semifinal matches feeding one bronze match.
  - **`PlayoffEngine`** extends `findBracketMatch` to also check `bracket.bronzeMatch` (tagging the result `isBronze`), and introduces a shared `writeBackMatch` helper that replaces three near-duplicated `rounds.map(...)` blocks across `startMatch`/`pauseMatch`/`resumeMatch` with one — a genuine simplification that also makes those three methods (plus `recordWalkover`) work on the bronze match automatically, no further special-casing needed. `updateBracket()` branches on `found.isBronze`: a normal round match still calls `advanceWinner` exactly as before, and additionally (only when the completed match was in the semifinal round) seats the loser into the bronze match via `populateBronzeMatch`; the bronze match itself, when completed, sets `thirdPlace`/`fourthPlace` and never advances anyone anywhere. **Champion/runner-up are set the instant the Final completes**, independent of whether Bronze (if enabled) has finished — but the bracket only locks (`status: "completed"`) once **both** are done (`isTournamentComplete` gained a one-line bronze check) — verified both orderings (Final-before-Bronze and Bronze-before-Final) produce identical final state. `reopenBracket` clears `thirdPlace`/`fourthPlace` alongside `champion`/`runnerUp` when a bronze match exists.
  - **`CourtAssignmentService`**'s `collectMatches`/`updateMatchIn` and `lib/tournament.js`'s `savePlayoffMatchResult` match-lookup all gained a one-line bronze-match fallback so court assignment, auto-fill, and rating hooks work on it identically to any other match. `PlayoffMatchService` needed **zero changes** — it was already fully generic over matchId.
  - **`TournamentReportService`**: `podiumFor`/`generateTournamentSummary` gained Third/Fourth Place (podium-order, now with 🥇🥈🥉🏅 labels); `generatePlayerStatistics` folds bronze-match participation into matches-played/wins/losses/points and flags Third/Fourth Place in Final Placement; `generatePlayoffReport` appends a Bronze Medal Match row; `generateTournamentTimeline` adds a Bronze Medal Match Completed event and — since `bracket.completedAt` now only stamps once the whole tournament (bronze included) locks — reads Champion Declared's timestamp off the Final match's own `completedAt` instead, so it stays accurate regardless of which match (Bronze or Final) finishes last. PDF/CSV export and Tournament History inherited all of this for free (format-agnostic export layer, reused `TournamentReportsView`).
  - **UI**: new "Bronze Medal Match" Enabled/Disabled toggle in Tournament Settings (right after "Playoff enabled", locked once a bracket match starts — same phase boundary as `advancesPerPool`). `TournamentBracketView` renders the bronze match as its own clearly-labeled column (🥉 Bronze Medal Match) positioned between the Semifinals and Championship Match columns, reusing `BracketMatchCard` unchanged; `BracketViewModel` enriches it with `.state` the same way round matches are enriched. Both the Bracket tab and Tournament Dashboard Overview gained 🥉/🏅 cards alongside the existing 🥇/🥈.
  - **Verification**: 43/43 new Node assertions (unit test for `populateBronzeMatch`'s slot logic, disabled-by-default backward compatibility, settings lock timing, both completion orderings, validation-after-completion, all report types, reopen, and the 2-team no-semifinal edge case) plus the 42-assertion Open Play acceptance suite, both green. Browser-verified end to end: seeded an 8-player/2-pool tournament, enabled Bronze Medal Match via Settings, played one semifinal (confirmed the loser seats into a still-locked bronze card while the winner correctly advances to the Final), then completed everything and confirmed the full 🥇🥈🥉🏅 podium across Overview, Tournament Summary, Playoff Results, Player Statistics, and Timeline, zero console errors throughout.
  - **Not implemented, per spec**: Consolation Bracket, Double Elimination, Best-of-3 Finals, Manual Seeding, Wild Card Qualification, Best Third Place, Manual Qualification Override — all untouched, left unchecked in `FEATURES.md` alongside the newly-added `- [ ] Placement Matches` line.
- **Sprint 7 — Manual & Advanced Seeding, a genuinely new feature (the second of this sprint series, after Bronze Medal Match, that wasn't already secretly built) — expands `BracketSeeding.js` from a single hardcoded function into five real Strategy classes.** The earlier audit had already flagged `BRACKET_SEEDING_METHODS` as a one-method registry with the extension seam ready but unused; this task fills it in.
  - **The core architectural fork**: bracket generation today is a fully synchronous hot path (`RoundRobinEngine.updateMatchResult` auto-generates the instant pools finish), but Rating-Based Seeding needs player ratings, which only exist in async storage, and Manual Seeding explicitly requires the bracket to *wait* for the organizer rather than fire automatically. Resolved by keeping auto-generation exactly as-is (zero behavior change) **only** for the default (`seedingMethod === "standardCrossPool"`); every other method (Random/Snake/Rating/Manual) leaves `tournament.bracket` null even once qualification is ready, until the organizer explicitly clicks **Generate Bracket** on the new Seeding tab — an async UI action that can freely await fetching ratings first. `tournament.seedingMethod` defaults to `"standardCrossPool"`, so every tournament created before this field existed keeps auto-generating exactly as before, same precedent as `playoffEnabled`/`bronzeMatchEnabled`.
  - **Five Strategy classes in `BracketSeeding.js`**, all extending a common base (`generateSeeds()`, `validateSeeds()`, `previewBracket()` — the last shared in the base class since every strategy previews round 1 the same way): `StandardCrossPoolSeedingStrategy` (wraps the original function byte-for-byte unchanged), `RandomSeedingStrategy` (Fisher-Yates driven by a small seeded PRNG — `mulberry32`, since `Math.random()` has no seed hook — repeatable given the same `context.seedValue`), `SnakeSeedingStrategy` (same rank-tier grouping Standard uses, but alternates sort direction per tier — the classic "snake draft" meaning, which is what actually differentiates it from Standard once there are 3+ pools; the spec's own example didn't demonstrate this distinctly, so this interpretation was flagged and confirmed with the organizer before implementing), `RatingSeedingStrategy` (sorts by average team-member `currentRating`, falling back to Standard Cross-Pool wholesale only when **zero** qualified participants have any rating on file), and `ManualSeedingStrategy` (reads `tournament.manualSeeds`, the only strategy with real `validateSeeds()` rules — missing assignments, duplicate seed numbers, out-of-range values). `PlayoffBracketGenerator.seedParticipants()`/`generateBracket()` pick the strategy from `tournament.seedingMethod` and never know which one is active — `createRounds()` (the actual round-1 mirror-pairing bracket builder) is completely untouched, confirming the seeding-vs-pairing split the spec's own Snake example implied.
  - **`lib/tournament.js`** gained `saveManualSeeds()` (persists organizer seed-number edits independently of generation), `buildSeedingContext()` (the one async step — fetches player ratings via `resolvePlayerIds`, now exported, for Rating mode; builds `{manualSeeds}` for Manual — shared between the real generate action and the Seeding page's live preview so they can never drift apart), and `saveGenerateBracket()` (validates via the strategy's own `validateSeeds()` before calling `PlayoffBracketGenerator` at all, throwing with every failing rule joined if invalid).
  - **New Seeding tab** (`TournamentSeedingView.jsx`): Qualified Participants/Pool/Rank/Seed Number/Seeding Method, editable seed-number inputs with live validation for Manual mode (explicit fields rather than drag-and-drop — the spec's own "or" made either acceptable), and a live bracket preview (round 1 pairings) that recomputes on every keystroke from the exact same `buildSeedingContext` the real Generate action uses. `TournamentBracketView`'s "seeded by Standard Cross-Pool Seeding" caption — found hardcoded during browser verification — now reads the tournament's actual `seedingMethod`.
  - **Verification**: 30/30 new Node assertions (every strategy's `generateSeeds`/`validateSeeds` in isolation, `previewBracket`'s shared round-1 mirroring, and three full end-to-end tournaments — default auto-generation unchanged, Manual blocked-then-generated after seed assignment, Rating fetching real stored ratings) plus the 42-assertion Open Play acceptance suite, both green. Browser-verified end to end: a 4-player tournament with Manual seeding selected, confirmed pool completion does NOT auto-generate a bracket, filled in seed numbers on the Seeding tab (watched the live preview update and the validation banner clear), generated the bracket, and confirmed the Bracket tab's actual matchups matched the preview exactly — zero console errors throughout.
  - **Not implemented, per spec**: Wild Card Qualification, Best Third Place, Manual Qualification Override, Double Elimination, Consolation Bracket, AI Court Optimization — all untouched.
- **Round Robin Scheduler — Tournament Manager's first format (schedule generation + display only; no scoring/standings/brackets yet).** Three independent pieces, deliberately not reusing Open Play's `courts`/`nextMatchups`/`matchHistory` shapes:
  - **Data model** (`src/lib/tournamentModel.js`): `Tournament` → `TournamentRound` → `TournamentMatch` (field lists now extended — see Tournament Engine Foundation above). Stored the same way as the Player Database — one KV record per tournament (`opl-tournament-{id}`, `TOURNAMENT_PREFIX`), never embedded in the session's own state blob. A session only carries a `tournamentId` pointer (`state.tournamentId`, additive/nullable — old sessions simply don't have one).
  - **Engine** (`src/engines/RoundRobinScheduler.js`): the classic circle/polygon method — fix one entrant, rotate every other entrant one seat per round — which guarantees every pair meets exactly once in the minimum number of rounds, with no search/backtracking needed. Deliberately does **not** extend `RotationEngine` (or, now, `TournamentEngine` — it predates that base class and is wrapped by `RoundRobinEngine` instead, see above): that interface is "generate the next matchup from a live, continuously-changing queue" (Open Play); this is "generate the entire schedule up front from a fixed, known entrant list" — a different problem. An "entrant"/Participant is generic (single player for Singles, pre-formed 2-player team for Doubles), so the engine itself never needs to know which mode produced it. Odd entrant count → a synthetic BYE entrant is added so the circle method's even-count requirement is met; the circle method's own symmetry then guarantees every real entrant draws exactly one bye, recorded as a real (visible, `isBye: true`) match rather than silently dropped. Court numbers are assigned by cycling `1..courtsCount` within each round — there's no "wait for a free court" sequencing logic, since this task only generates a schedule, not a live running order. A separate `pairIntoTeams` helper handles Doubles' player→team step (simple sequential pairing, not seeded — "Seeding" is already its own later `FEATURES.md` item); it throws rather than silently benching someone if the player count is odd, since deciding who sits out the whole tournament isn't this function's call to make.
  - **Orchestration** (`src/lib/tournament.js`): the only piece that knows about session players — builds entrants from `state.players` (via `pairIntoTeams` for Doubles), calls the engine, and persists the result.
  - **UI**: `TournamentScheduleView.jsx` (now nested inside the Tournament Dashboard's Schedule sub-tab — see above) always shows the Singles/Doubles toggle and a Generate/Regenerate button (kept visible even after a schedule exists, so switching modes doesn't require a dead end); regenerating creates a brand-new Tournament record with a new id rather than overwriting the old one in place, since nothing references match ids yet, so a failed regenerate (e.g. odd player count for Doubles) can surface an error without corrupting the currently-displayed schedule. Rounds render as expandable cards (reusing `HistoryView`'s round-card styles) showing Court, both teams, and a static `Pending`/`In Progress`/`Completed` status badge — the badge never transitions; nothing writes to `status`.
  - **Explicitly not built:** standings, match scoring, rankings, champion calculation, knockout brackets — all separate future `FEATURES.md` items.
- **Three rotation modes, chosen once at Create Session.** `state.rotationMode` ("continuous" | "winnerPool" | "progressiveSkill", default `"continuous"`) is selected on `CreateSessionScreen.jsx` (step 3, alongside venue name/court count) and baked into `initial.rotationMode` when `startSession` writes the session's first Supabase record — there's no later control to switch it, so it can't drift mid-session. Scorer shows the active mode inside a read-only **Session Information card** at the top of the page (`ScorerView.jsx` — Rotation Mode, Courts, Players, Waiting Players, Current Round; Rotation Mode is looked up from `ROTATION_MODES`, no setter passed down) rather than a dropdown, since the underlying rotation logic (`getRotationEngine`, `refreshNextMatchups`/`regenerateNextMatchups`, `isPoolingRotation`) is untouched by where the mode gets picked. "Current Round" is `matchHistory.length + 1` — the same round number `endMatch` would assign to the next completed match, so it always matches what Game History will show once that match ends. It still decides between:
  - **Continuous queue** (default) — the model described above: every court draws independently from one shared waiting queue.
  - **Winner Pool Rotation** — courts pair up by fixed adjacent position (court 1 & 2, 3 & 4, ...). When *both* courts in a pair have finished, `src/lib/winnerPoolRound.js`'s `resolveWinnerPoolMatch` pools their 4 winners into one new match (via `WinnerPoolRotationEngine`, always 1 beginner + 1 intermediate per team, avoiding the immediately-previous partner and opponent) and their 4 losers into another. Both courts in the pair open up (not redeployed to directly) and the two new matchups join the *back* of `nextMatchups` — so anyone else who's been waiting gets first claim on the now-open courts via the normal "Assign match"/"Fill all open courts" flow, and the just-finished 8 players don't just keep replaying each other while everyone else sits out. A court that finishes before its partner shows a distinct "WAITING" state (`court.awaitingPair`) with the final score locked in, rather than being emptied — see `CourtCard.jsx`. This is the one deliberate, explicitly-scoped exception to "continuous, not lockstep" above: it's a hard requirement of the mode itself (pooling needs both courts' results at once), not incidental round-based phrasing, and it's pairwise (only the 2 courts in a pool wait on each other) rather than a full-session lockstep. The odd court out, when the courts array has an odd length, falls back to the continuous model's normal per-court requeue.
  - **Progressive Skill Rotation** — selectable end-to-end (picker, engine wiring, matchup generation, standard continuous-queue court lifecycle *outside Mentorship — see below*) and now phase-aware. `state.expectedGamesPerPlayer` (organizer-configurable in Scorer, default 6) and `src/lib/progressiveSkillPhase.js`'s `calculateSessionProgress`/`getProgressivePhase` compute the average games-played-so-far across checked-in players as a percentage of that target, and classify it into one of three phases — Mentorship, Transition, Competitive, with organizer-configurable boundaries (`state.progressiveSkillThresholds`, default 0–30% / 31–60% / 61–100%; `buildPhases` clamps/orders any input into a valid, non-zero-width set of zones). The active phase is threaded through `refreshNextMatchups`/`regenerateNextMatchups` (as `phase`, computed by `progressiveSkillPhaseFor` — lives in `lib/progressiveSkillPhase.js` itself so headless callers like `RotationSimulationEngine` can compute it too, without importing a React component) into `ProgressiveSkillRotationStrategy.generateMatchups({ ..., phase })`:
    - **Mentorship** — pairing delegates straight to `BalancedRotationEngine` (composed, not duplicated): prioritize beginner+intermediate teams, avoid repeating a player's most recent partner, minimize repeated opponents — the same scoring Continuous queue mode uses. Court *lifecycle* also switches to Winner Pool Rotation's pairwise pooling mechanic while this phase is active (`isPoolingRotation` in `lib/winnerPoolRound.js` gates on `rotationMode === "winnerPool"` OR `rotationMode === "progressiveSkill" && phase === "mentorship"`): courts pair up 1&2/3&4/..., a court that finishes first holds ("WAITING") until its pair partner also finishes, then both courts' winners pool into one new match and both courts' losers into another (via the same `WinnerPoolRotationEngine`/`resolveWinnerPoolMatch` standalone Winner Pool Rotation uses), joining the *back* of `nextMatchups` rather than redeploying straight onto the same two courts. If the session's phase crosses out of Mentorship while a court is already held "awaitingPair", `endMatch` still resolves that pooling when its partner finishes (checked independently of the *current* phase) so a phase boundary crossed mid-pair can't strand a court in the held state forever.
    - **Transition** — delegates to `TransitionRotationEngine`. Team formation: avoid a player's most recently repeated partner (same recency scale as Mentorship), plus a soft +30 bonus for a beginner+intermediate pairing — a preference, not a requirement, so a same-skill team is never blocked when it's genuinely the best available option (e.g. every mixed pairing would repeat a recent partner). Matchup formation: avoid repeated opponents (same recency scale), then use Performance Rating (`lib/performanceRating.js`) as a match-quality factor — the two teams' composite rating (average of their players' ratings, with not-yet-rated players treated as a neutral 50) is compared, and a bigger gap is penalized, so a hot team is less likely to be paired against a cold one. Same randomized-restart hedge (`bestOfAttempts`) as `BalancedRotationEngine` against greedy tie-breaking.
    - **Competitive** — delegates to `CompetitiveRotationEngine`. Ignores the original beginner/intermediate labels entirely — no bonus, no requirement. Team formation is primarily by Performance Rating closeness (players end up tiered with others at a similar level right now, weighted ~2x a full partner-recency swing so it dominates), with partner-recency avoidance still able to outweigh a small rating gap when a genuine repeat-partner conflict exists. Matchup formation avoids repeated opponents (same recency scoring as every other engine here); no separate rating-balancing pass, since teams are already rating-tiered from the partner stage. Same randomized-restart hedge (`bestOfAttempts`) as the other engines.
  - **Progressive Skill Rotation's UI** lives in `src/components/ProgressiveSkillPanel.jsx`, rendered in Scorer only when this mode is active: an animated phase badge (CSS `@keyframes phasePop`, restarted on every phase change via `key={activePhase.key}` forcing a remount), a 3-zone progress bar (`buildPhases`-driven zone widths, a pulsing marker at the current session-progress position, `transition: left 0.5s ease` so it glides rather than jumps), a collapsible Settings panel (expected games/player, and the two phase-boundary percentages — reuses the same `expectedGamesPerPlayer` input from before, now alongside the new threshold inputs), and a per-phase match-count stats row. Match counts come from `matchHistory` entries tagged with the phase they were actually played under (`endMatch` computes this from the *pre-match* state, since games-played only increments after a match ends) — so the stats reflect the session's real history, not just whatever phase happens to be active right now.
- **Session-only Performance Rating.** `src/lib/performanceRating.js`'s `calculatePerformanceRating(player)` derives a single 0-100 number from stats already tracked per player — wins, losses, games played, current streak, and point differential (`pointsFor - pointsAgainst`) — weighted so win rate dominates, with average point differential and streak as smaller adjustments; `null` until a player's first completed game. It's not a real skill rating (like DUPR), just an at-a-glance "who's playing well today" indicator. Shown as a colored badge next to each player in Standings (with a new RTG column) and in Scorer's waiting players list. Session-only by construction — every input stat already resets to 0 when a session is created (see the `players` object built in `createSession`) and nothing persists beyond that session's own storage key, so no separate reset logic was needed.
- **Standings: sortable columns, computed client-side.** `StandingsView.jsx` derives `gp` (`wins + losses`) alongside the existing `diff`/`performance` per-row fields — no new state field, always in sync with `matchHistory` since it's recomputed from `players` on every render. Clicking a sortable column header (`GP`/`W`/`L`/`+/-`/`RTG`) cycles `null → "asc" → "desc" → null` for that column via two `useState`s (`sortKey`, `sortDir`); only one column can be active since they share the same state pair. With no active sort, rows fall back to the default order — highest Rating, then highest Wins, then highest point differential, then alphabetical name (`defaultCompare`) — matching (and formalizing) the ranking the page always used. Sorting is a plain in-memory `.sort()` over already-loaded `players`, so it's instant and needs no round-trip; `SORT_COLUMNS` (an array of `{ key, label, getValue }`) is the single place a sortable column is defined, driving both the header buttons and the comparator.
- **Organizer controls in Scorer.** Manual swap: `reassignTeams`/`substitutePlayer` edit a *live* court's teams; `reassignMatchup`/`substituteInMatchup` edit an upcoming (not-yet-deployed) matchup — both draw only from players not already locked into something else, so the two features can't fight over the same player. Lock: `toggleLockMatchup` protects one matchup from `regenerateMatchups` (a full "rebuild everything not-locked" rerun of the active rotation engine over the current waiting pool) while everything else about it (Fix teams/Substitute) stays editable. Preview before publishing: `nextMatchups` is a pre-built, scorer-reviewable list — "Assign match"/"Fill all open courts" is the only thing that deploys a matchup from that list onto an actual court, so nothing goes live without appearing in Scorer first. Undo: two one-shot, device-local snapshots (not synced to Supabase, so they're a quick "oops" for whoever just clicked, not a durable multi-device feature) — `regenerateSnapshot` (`undoRegenerate`, restores `nextMatchups` to before the last Regenerate) and `lastRoundSnapshot` (`undoLastRound`, restores the *entire* pre-match state — court back to live with its original teams/score, players' stats and rotation history reverted, `matchHistory` entry removed, `queueIds` back to not having those players requeued — from right before the last "End match"). Both are cleared by `clearOneShotSnapshots()` at every point that could make restoring them unsafe (deploying a matchup, editing a matchup, another regenerate/end-match, skipping/removing a player, a new check-in, leaving the session) so neither can ever silently resurrect a player or round that's since moved on.
- **Replacement sourcing: Waiting Queue + Upcoming Matchups, and "Move to Queue".** Real Open Play sessions often need to pull a replacement from someone already scheduled into a later matchup, not just the waiting queue. `lib/utils.js`'s `buildReplacementCandidates(nextMatchups, unassignedPlayers, players, excludeMatchupId)` returns `{ waiting, upcoming }` — `upcoming` entries are tagged with `scheduledLabel` (the same "Next up" / "Then · matchup N" text Scorer already shows above each matchup card, so a candidate's tag always matches what's on screen) and `matchupId`; `excludeMatchupId` leaves out the matchup currently being edited so a substitution never offers one of its own teammates/opponents as its own replacement. `PlayerPicker` renders both pools as separate labeled sections. Picking a candidate from `upcoming` runs `dissolveMatchupIfReserved(nextMatchups, playerId, exceptMatchupId)` first — a matchup can't exist with only 3 players, so pulling one out always tears the *whole* matchup down, freeing all 4 back to the unassigned pool (they never left `queueIds` — see `reservedMatchupIds` — so nothing needs re-adding). The next `refreshNextMatchups` (every `save()` runs one) automatically rebuilds fresh matchups from that larger pool via the session's *active* rotation engine, so Progressive Skill Rotation's pairing logic decides who plays whom next — this doesn't bypass or duplicate that algorithm, and never touches `matchHistory`, `players` stats, or anything about a completed match. Both `substitutePlayer` (live court) and `substituteInMatchup` (upcoming matchup) call `dissolveMatchupIfReserved` on their incoming player before making the swap, so either flow works identically whether the replacement came from the queue or another matchup. **"Move to Queue"** (`moveToQueue(playerId)`, a "↩" button on every `PlayerChip` inside an upcoming matchup — i.e. every player who hasn't started their next scheduled game) is the same `dissolveMatchupIfReserved` call on its own: an organizer-initiated way to pull someone out and make them immediately available, without pairing it with an immediate substitution elsewhere.
- **Manual Court Assignment.** Each court has `assignmentMode` (`"automatic"` | `"manual"`, toggle only shown while `status === "open"`) and `manualLocked`, added to `emptyCourt` in `lib/constants.js`. Switching a court to Manual clears its `teamA`/`teamB` and lets the organizer build a draft directly in those same arrays — filling a slot (`setManualCourtPlayer`, sourced from the same `PlayerPicker`/`buildReplacementCandidates` waiting-queue + upcoming-matchups pools the replacement-sourcing feature uses, including `dissolveMatchupIfReserved` for a pick coming out of an upcoming matchup) requires no data transformation once locked, since a manual court's draft already has the exact shape a live court needs. `lib/utils.js`'s `manuallyReservedIds(courts)` is the single source of truth for "this player is spoken for by a manual court" — `save()` filters it out of the pool passed to `refreshNextMatchups` on *every* state change (not just an explicit action), so the automatic rotation engine permanently ignores anyone drafted or locked into a manual court, matching "should integrate seamlessly ... and only affect future match generation." `lockManualCourt` validates exactly 2 unique players per side (4 unique total) before flipping `status` to `"live"`/`manualLocked: true` — from there it behaves exactly like any other live court (same score/end-match/Fix teams flow; `matchHistory` and player stats are untouched by any of this). `unlockManualCourt` (available on a live, still-unfinished manually-locked court) reverses the lock, returning its 4 players to `queueIds` and clearing the draft back to an empty Manual court — a match reaching `"finished"`/`endMatch` releases the lock on its own via the normal flow instead. `generateRemainingCourts` ("Generate Remaining Courts" button) is the one-click version of the organizer workflow's last step: rebuilds `nextMatchups` from scratch (`regenerateNextMatchups`, same call "Regenerate matchups" uses) over a pool pre-filtered by `manuallyReservedIds`, then deploys the results onto every open *automatic* court, leaving manual courts (draft or locked) completely alone. UI: the Automatic/Manual toggle and slot editor live in `CourtCard.jsx` (shown only pre-lock), with a "🔒 Manual Assignment" badge replacing the normal status tag once locked, and an "Unlock" button alongside "Fix teams" while live.
- **Game History — read-only, grouped by round.** `src/components/HistoryView.jsx` (a new "History" nav tab, alongside Live Board/Check In/Standings/Scorer) renders `state.matchHistory` — already-stored, already-immutable completed-match records — with zero writes back to state, so it can never itself change what actually happened. Grouped into expandable round cards (`matchRecord.round`, which — consistent with this app's continuous, non-lockstep architecture, see "Matchups are built continuously" above — is the order matches actually *finished* in, not a synchronized "everyone plays round N together" batch; most sessions show one game per round card, reflecting that courts run independently); the most-recently-completed round starts expanded, every earlier one collapsed, with a click to toggle any round. Each match card shows court, both teams (score, winner highlighted), and completion time; clicking any player name opens a `PlayerHistoryPanel` — every game that player's been in, their partner/opponents/result per round, and a games/W-L/courts-played summary, computed by filtering `matchHistory` for that player id, nothing pre-aggregated or cached. A combined search box (matches player name, round number, or court number) plus separate round/court dropdown filters narrow the round cards shown. "Match Duration" isn't tracked — the app doesn't currently record a match's start time anywhere, only `endedAt` — so it's omitted rather than faked; would need a `startedAt` added at deploy-time (`fillCourt`/`lockManualCourt`/etc.) if a future task wants it. CSV/JSON export (`exportHistoryCsv`/`exportHistoryJson`) resolve player ids to names at export time only, client-side `Blob` + anchor-download, no server round-trip — `matchHistory` itself keeps storing ids, so a later name edit can't retroactively rewrite exported history. Future-compatibility note: `matchHistory` entries were already shaped for reuse (round/court/team ids/scores/winner/timestamp/phase) before this feature existed — History adds a read surface over that existing shape rather than a new one, so it stays reusable for player profiles/season stats/rankings/reports later.
- **Quick "Won" scoring.** `declareWinner(courtIdx, team)` skips point-by-point scoring for casual games — it sets the declared winner's score straight to 11 and the other team's to 0 and marks the court "finished", exactly as if `adjustScore` had been clicked eleven times. A "Won" button (`TeamRow`'s `onDeclareWinner`) sits next to each team; it's a shortcut into the existing score/status/`endMatch` flow, not a separate code path, so wins recorded this way behave identically everywhere (stats, `matchHistory`, rotation history) to a normally-scored 11-0 game.
- **Rotation simulation — headless engine, two front ends (CLI + a Developer page).** `src/lib/simulation/RotationSimulationEngine.js`'s `runSimulation(config)` plays out a complete Open Play session under Progressive Skill Rotation entirely in memory (browser or Node — it's plain JS, no server round-trip) and returns a structured result (`playerSummaries`, `matchLog`, `roundLog`, `phaseCounts`, `stopReason`, `fairnessStats`, etc.). It genuinely reuses the live app's matchmaking — `getRotationEngine`/`refreshNextMatchups` (which dispatch through `ProgressiveSkillRotationStrategy` to `BalancedRotationEngine`/`TransitionRotationEngine`/`CompetitiveRotationEngine` per phase), `progressiveSkillPhaseFor`, and the Winner Pool Rotation pooling mechanic (`isPoolingRotation`/`resolveWinnerPoolMatch`) that Mentorship phase borrows — none of that is reimplemented. What the engine *does* supply itself is the round-by-round orchestration a live UI drives one click at a time (`fillAllOpenCourts`, `endMatchAt`, mirroring `PickleballOpenPlay.jsx`'s `fillAllCourts`/`endMatch` field-for-field) — those aren't pure functions in the app today, since they're React state setters, so the simulation reimplements just that glue, not the algorithm. A round: refresh matchups for the current phase → fill every open court → resolve a plausible random score for each live court → end each match (stats, rotation history, pooling) → `releaseStalePairings` → repeat until every player has reached `expectedGamesPerPlayer` or a safety cap (`maxRounds`, default 300) or a genuine deadlock (too few players left to fill a court) is hit.
  - **Random player counts.** `randomizePlayers: true` (config) ignores `playerCount` and calls `randomPlayerCount()` — an inclusive random integer in `[MIN_RANDOM_PLAYERS, MAX_RANDOM_PLAYERS]` (8–24, a realistic open-play turnout range) — picked fresh for that run. `cfg.playerCount` is overwritten with whatever was actually used so it's reported accurately everywhere downstream (report, UI, comparison table).
  - **CLI**: `node scripts/run-simulation.mjs` (`--players=`, `--courts=`, `--games=`, `--skillSplit=`, `--mentorshipMax=`, `--transitionMax=`, bare `--randomPlayers` flag — `--courts` also accepts a comma-separated list, e.g. `--courts=2,3,4`, to run the same session at each court count back-to-back and print a comparison table; when combined with `--randomPlayers` the headcount is resolved once and shared across all court counts so the comparison stays apples-to-apples) or import `runSimulation`/`printSimulationReport` directly.
  - **Developer page**: `src/components/DeveloperView.jsx`, reached via a "Developer? Run the rotation simulator →" link on the landing screen (same pattern as the existing "Organizer? Manage access codes" link) — a new `screen === "developer"` state in `PickleballOpenPlay.jsx`, no session/auth/Supabase involved. A small form (players, courts, games/player, beginner %, a randomize-players checkbox, a compare-2/3/4-courts checkbox) calls `runSimulation` directly in the browser on click and renders the result: summary line, phase counts, a Fairness Score badge, and a standings table reusing `StandingsView`'s table styles (`standingsTable`/`standingsHeadRow`/`standingsRow`/etc. — same look, no new table styling invented). Deliberately simple/read-only; no export, no persistence, nothing saved anywhere.
  - **Fairness statistics + score.** `calculateFairnessStats(playerSummaries)` reduces the roster's games-played counts to `{ minGames, maxGames, avgGames, stdDevGames, fairnessScore }` — population standard deviation (the roster *is* the whole population for the session, not a sample), rounded to 2 decimal places. `fairnessScore` condenses that into one 0-100 number for an at-a-glance read (100 = everyone played exactly the same number of games), built from the coefficient of variation (`stdDevGames / avgGames`, not raw stdDev) so it stays meaningful across very different `expectedGamesPerPlayer` settings — a stdDev of 1 game means something different at "3 games/player" than at "12 games/player," and the CV normalizes for that; clamped to `[0, 100]`, `100` when `avgGames` is 0. The multi-court comparison table (`--courts=2,3,4`, or the Developer page's compare checkbox) surfaces min/max/stdDev/score side by side so a widening spread at a given court count is easy to spot at a glance.
  - **`releaseStalePairings`: a simulation-only deadlock fix for Winner Pool Rotation's pairwise hold.** Real sessions always have enough waiting players to eventually fill both courts in a pair, but a *simulated* one can be configured with more courts than the player count can ever fill simultaneously — e.g. 16 players across 4 courts (paired 1&2, 3&4) can run at most 4 matches, but 12 players across 4 courts can only ever run 3, so one court in a pair permanently never gets a 4th match. Without this, the live app's ordinary pairwise-hold behavior (a finished court waits, `awaitingPair`, for its partner) would hold that court's players forever — they'd never be requeued, and would sit at whatever games count they had when the deadlock started rather than reaching `expectedGamesPerPlayer`. Called once per round after that round's matches end: any court still `awaitingPair` whose partner is (still) `"open"` — meaning this round's fill pass already had its one chance and the partner didn't get deployed a match — is force-released back to the queue immediately, rather than waiting indefinitely. This is orchestration the simulation adds for itself; it doesn't change `resolveWinnerPoolMatch`/`isPoolingRotation` or any pooling behavior the live app relies on.
- **PWA: installable, offline app shell, CONNECT.PH-branded.** `vite-plugin-pwa` (configured in `vite.config.js`) generates the Web App Manifest (`manifest.webmanifest` — name "CONNECT.PH Open Play Manager", short name "Open Play", `display: "standalone"`, theme/background colors matching `--color-primary`/`--color-bg`) and a Workbox service worker (`dist/sw.js`) at build time; both are auto-injected into `index.html` (`injectRegister: "auto"` adds the registration script, no changes needed in `src/main.jsx`). The service worker precaches the built app shell — JS/CSS bundles, `index.html`, icons, favicons — via `globPatterns`, and `navigateFallback: "/index.html"` serves that cached shell for any in-app navigation while offline (the app has no server-side routes, so the shell *is* the whole app once React takes over). Google Fonts (stylesheet + woff2 files) get a `CacheFirst` runtime-caching rule so custom fonts still render offline after a first successful load. Deliberately **no** runtime caching rule for Supabase requests — session/score data must always come from the network when available, never served stale; offline just means the shell still opens, not that live data works without a connection. iOS doesn't install from the Web App Manifest alone, so `index.html` also carries the Apple-specific meta tags (`apple-mobile-web-app-capable`, `apple-touch-icon`, etc.) needed for "Add to Home Screen" to launch standalone there. All icon/favicon assets (`public/icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `favicon-16x16.png`, `favicon-32x32.png`, `favicon.ico`, and `public/favicon.svg`) share one CONNECT.PH-inspired mark — a navy field, an open orange ring evoking the logo's "C", and a small orange connector dot at center — generated by `scripts/generate-pwa-icons.mjs`, a dependency-free script that hand-encodes both PNG (raw RGBA + `zlib.deflateSync` + a small CRC32 table) and ICO (a thin container wrapping the same PNG bytes) formats. Re-run it if the brand colors ever change (keep it in sync with `styles.js`'s `--color-primary`/`--color-secondary` and `favicon.svg`). No Vercel config changes needed — `dist/` (including `sw.js`, `manifest.webmanifest`, `registerSW.js`) is just more static output from `vite build`, which Vercel already serves as-is.

## Tech stack

- [React](https://react.dev/) 18
- [Vite](https://vitejs.dev/) 5 — dev server and bundling
- [Supabase](https://supabase.com) (`@supabase/supabase-js`) — Postgres storage + Realtime
- [lucide-react](https://lucide.dev/) — icons
- Plain inline styles (no CSS framework) — design tokens live in `src/styles.js`
- [vite-plugin-pwa](https://vite-pwa-org.netlify.app/) — Web App Manifest + Workbox service worker generation

## Known limitations

- No real authentication (PIN-gated roles only, unenforced server-side)
- The Supabase anon key has full read/write access to session and access-code data (see `backend/README.md`)
- Player photos stored as base64 thumbnails in the same table as everything else
- Access codes and session data share the same trust boundary
- All three Progressive Skill Rotation phases (Mentorship, Transition, Competitive) now have real pairing logic — see Architecture above
- Adding a *fourth* rotation strategy is straightforward — the Strategy pattern (`RotationEngine` subclass + a case in `getRotationEngine`) and the mode picker both already support more being added
- Undo is one level deep and device-local for both "Undo regenerate" and "Undo last round" — neither is a full multi-step history/redo stack, and neither syncs to other devices; substituting or removing a player remains one-way with no undo
- Winner Pool Rotation's pairwise synchronization (holding a finished court until its pair partner also finishes) is resolved client-side on whichever device clicks "Confirm result" last; two scorers on different devices confirming both paired courts within the same ~1s Realtime sync window could theoretically race — not handled, same trust model as the rest of the app's optimistic concurrency
- No dedicated stats screen for partner/opponent frequency or court usage — that history is tracked per-player (`partnerCounts`, `opponentCounts`, `courtCounts`) but only Standings (games/wins/losses/streak) is surfaced in the UI today
- PWA offline support covers the app shell only (it can still *open* with no connection) — live session/score data is never cached and always requires the network, same trust model as the rest of the app
- PWA icons/favicons (`scripts/generate-pwa-icons.mjs`) are a simplified, hand-drawn CONNECT.PH-inspired mark (navy field, orange ring + dot), not a pixel-accurate render of the full logo file (the logo's wordmark and plug illustration can't survive down to a 16x16 favicon anyway) — swap in real exported assets from the logo source file if/when one's available
- No dedicated Apple splash-screen images (the `apple-touch-startup-image` matrix of per-device sizes) — iOS auto-generates a basic splash from the manifest's icon/background_color/name when none is provided, which was judged good enough against the effort of maintaining a full device-size matrix by hand

These are documented in more detail in [README.md](README.md) and [backend/README.md](backend/README.md).
